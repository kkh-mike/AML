Comparing:     [ 1 ] to [ 2 ]
[ 1 ] C:\Users\1091707\Downloads\dbo.GLSPEC_SyncDJToInternalList.StoredProcedure_bef.sql
[ 2 ] C:\Users\1091707\Downloads\dbo.GLSPEC_SyncDJToInternalList.StoredProcedure.sql
----------------------------------------------------------------------------------------
=======
1	  	USE [BSADB]
2	  	GO
=======
3	* 	/****** Object:  StoredProcedure [dbo].[GLSPEC_SyncDJToInternalList]    Script Date: 2025/12/22 上午 09:59:19 ******/
 	* 	/****** Object:  StoredProcedure [dbo].[GLSPEC_SyncDJToInternalList]    Script Date: 2026/5/21 上午 11:30:40 ******/
=======
4	  	SET ANSI_NULLS ON
5	  	GO
6	  	SET QUOTED_IDENTIFIER ON
7	  	GO
8	  	
9	  	
10	  	ALTER PROCEDURE [dbo].[GLSPEC_SyncDJToInternalList]
11	  	@GroupId BIGINT
12	  	AS
13	  	BEGIN
14	  	   DECLARE @OtherIdentMthCode VARCHAR(2),
15	  	         @Filter_Alt_Str INT
16	  	         
17	  	   SELECT @OtherIdentMthCode = IDENTCODE FROM dbo.IDENTMTHD_DIM WHERE OTHIND = 'Y' 
18	  	   SELECT @Filter_Alt_Str = CtrlVal FROM dbo.PO_DIMData WHERE Module = 'DJ' AND CtrlKey = 'DJ_ToGLSPEC_ALT_STR'
19	  	
20	  	   IF OBJECT_ID('tempdb..#tempDJBlacklist') IS NOT NULL
21	  	       DROP TABLE #tempDJBlacklist
22	  	   IF OBJECT_ID('tempdb..#tempDJBlacklistDetails') IS NOT NULL
23	  	       DROP TABLE #tempDJBlacklistDetails
24	  	
25	  	   CREATE TABLE #tempDJBlacklist (EntNum INT NOT NULL, SdnName NVARCHAR(350) NOT NULL, UpdatedDate VARCHAR(8) NOT NULL)
26	  	
27	  	   CREATE TABLE #tempDJBlacklistDetails(EntNum INT NOT NULL, SdnName NVARCHAR(350) NOT NULL, UpdatedDate VARCHAR(8) NOT NULL, 
28	  	      Address NVARCHAR(400), City NVARCHAR(200), Country NVARCHAR(100), IdentType VARCHAR(2), IdentNumber NVARCHAR(44), DOB VARCHAR(8),Gender NVARCHAR(7))
29	  	   
30	  	   INSERT INTO #tempDJBlacklist ( EntNum, SdnName, UpdatedDate )
31	  	   SELECT  A.EntNum, A.SdnName, MAX(A.UpdatedDate) AS UpdatedDate 
32	  	   FROM BSADB.dbo.DJ_List_Data_A A 
33	  	   WHERE A.NamePropertyBitCode & 1 > 0 AND A.AltStrengthBitCode & @Filter_Alt_Str <> 0
34	  	   GROUP BY A.EntNum, A.SdnName 
35	  	
36	  	   ;WITH TempAddress AS (
37	  	      SELECT A.EntityId, A.Address, A.City, A.AddressCountry, ROW_NUMBER() OVER(PARTITION BY A.EntityId ORDER BY A.Id) 'RowId'
38	  	      FROM BSADB.dbo.DJ_Address A WITH(NOLOCK) 
39	  	      WHERE A.Deleted = 0  AND EXISTS(SELECT 1 FROM #tempDJBlacklist X WHERE A.EntityId = X.EntNum)
40	  	   ), TempIdent AS(
41	  	      SELECT EntityId, CASE WHEN ISNULL(IdentificationType,'') = '' THEN NULL ELSE @OtherIdentMthCode END 'IdentificationType', IdentificationNumber, ROW_NUMBER() OVER(PARTITION BY A.EntityId ORDER BY A.Id) 'RowId' 
42	  	      FROM BSADB.dbo.DJ_IdentificationTypeInfo A WITH(NOLOCK) WHERE A.Deleted = 0 AND EXISTS(SELECT 1 FROM #tempDJBlacklist X WHERE A.EntityId = X.EntNum)
43	  	   ), TempDOB AS (
44	  	      SELECT EntityId, InfoDay, InfoMonth, InfoYear, ROW_NUMBER() OVER(PARTITION BY A.EntityId ORDER BY A.Id) 'RowId' 
45	  	      FROM BSADB.dbo.DJ_DateTypeInfo A WITH(NOLOCK) WHERE A.DateType = 'Date of Birth' AND A.Deleted = 0 AND EXISTS(SELECT 1 FROM #tempDJBlacklist X WHERE A.EntityId = X.EntNum)
46	  	   ), TempGender AS (
47	  	      SELECT A.EntityId, CASE 
48	  	         WHEN ISNULL(A.Gender,'') = '' THEN NULL 
49	  	         WHEN ISNULL(A.Gender,'') = 'Male' THEN 'M' 
50	  	         WHEN ISNULL(A.Gender,'') = 'Female' THEN 'F'
51	  	      END 'Gender' , ROW_NUMBER() OVER(PARTITION BY A.EntityId ORDER BY A.EntityId) 'RowId' 
52	  	      FROM BSADB.dbo.DJ_Entity A WITH(NOLOCK)
53	  	      WHERE a.Deleted = 0 AND EXISTS(SELECT 1 FROM #tempDJBlacklist X WITH(NOLOCK) WHERE a.EntityId = x.EntNum)
54	  	   )
55	  	   INSERT INTO #tempDJBlacklistDetails ( EntNum, SdnName, UpdatedDate, Address, City, Country, IdentType, IdentNumber, DOB, Gender )
56	  	   SELECT A.EntNum, A.SdnName, A.UpdatedDate, B.Address, LEFT(B.City,100), LEFT(B.AddressCountry,100),
57	  	      C.IdentificationType, LEFT(C.IdentificationNumber,44) , 
58	  	      CASE WHEN D.InfoDay IS NULL OR D.InfoMonth IS NULL THEN D.InfoYear ELSE   CONVERT(VARCHAR(8),dbo.TryParseDate(D.InfoYear + ' ' + D.InfoMonth + ' ' + D.InfoDay), 112) END, E.Gender
59	  	   FROM #tempDJBlacklist A 
60	  	      LEFT JOIN TempAddress B ON A.EntNum = B.EntityId AND B.RowId = 1 
61	  	      LEFT JOIN TempIdent C ON A.EntNum = C.EntityId AND C.RowId = 1 
62	  	      LEFT JOIN TempDOB D ON A.EntNum = D.EntityId AND D.RowId = 1 
63	  	      LEFT JOIN TempGender E ON A.EntNum = E.EntityId AND E.RowId = 1
64	  	
65	  	
66	  	   ;WITH TargetTable AS (
67	  	      SELECT A.spec_name, A.updated_date, A.remark, A.TaxID, A.IDENT, A.IDENTO, A.TaxIDCD, A.IDNUM, A.DInd, A.creUserID, A.STATE, A.ADDRESS, A.COUNTRY,
68	  	               A.GROUPID, A.SOURCE, A.EnterFlag, A.ApprovedBy, A.ApprovedDate, A.DelUserID, A.DelDate, A.ApproveDelUserID, A.ApproveDelDate, A.creDate,
69	  	               A.updated_date_UTC, A.ApprovedDate_UTC, A.DelDate_UTC, A.ApproveDelDate_UTC, A.creDate_UTC, A.ent_num, A.DOB, A.Gender
70	  	      /*A.spec_name, A.updated_date, A.remark, A.DInd, A.creUserID, A.STATE, A.ADDRESS, A.COUNTRY,
71	  	            A.GROUPID, A.SOURCE, A.EnterFlag, A.ApprovedBy, A.ApprovedDate, A.DelUserID, A.DelDate, A.ApproveDelUserID, A.ApproveDelDate, A.creDate,
72	  	            A.updated_date_UTC, A.ApprovedDate_UTC, A.DelDate_UTC, A.ApproveDelDate_UTC, A.creDate_UTC, A.ent_num*/
73	  	      FROM BSADB.dbo.OFAC_GLSpec A WHERE A.GROUPID = @GroupId --AND A.DInd = 'N'
74	  	   )
75	  	   MERGE TargetTable T 
76	  	   USING (SELECT EntNum, SdnName, UpdatedDate, Address, City, Country, IdentType, IdentNumber, DOB, Gender FROM #tempDJBlacklistDetails) S
77	  	   ON T.spec_name = S.SdnName AND T.remark = CAST(S.EntNum AS VARCHAR(100))
78	  	   WHEN NOT MATCHED BY TARGET THEN
79	  	      INSERT ( spec_name, updated_date, remark, DInd, creUserID, STATE, ADDRESS, COUNTRY, GROUPID, IDENT, IDNUM, DOB, Gender,
80	  	                SOURCE, EnterFlag, ApprovedBy, ApprovedDate, DelUserID, DelDate, ApproveDelUserID, ApproveDelDate, creDate, updated_date_UTC,
81	  	                ApprovedDate_UTC, DelDate_UTC, ApproveDelDate_UTC, creDate_UTC)
82	  	      VALUES(S.SdnName, S.UpdatedDate, S.EntNum, 'N', 'System Admin', ISNULL(S.City,''), S.Address, S.Country, @GroupId, S.IdentType, S.IdentNumber, S.DOB, 
83	  	            S.Gender,
84	  	            NULL, 'D', 'System Admin', GETDATE(), 'System Admin', GETDATE(), 'System Admin', GETDATE(), GETDATE(), GETDATE(), 
85	  	            GETUTCDATE(), NULL, NULL, GETUTCDATE())
86	  	   WHEN NOT MATCHED BY SOURCE AND T.DInd = 'N' THEN 
87	  	      UPDATE SET T.DInd = 'Y', T.DelUserID = 'System Admin', T.DelDate = GETDATE(), T.ApproveDelUserID = 'System Admin', T.ApproveDelDate = GETDATE(),
88	  	            T.DelDate_UTC = GETUTCDATE()
89	  	   WHEN MATCHED AND (T.DInd = 'Y' OR S.UpdatedDate <> T.updated_date) THEN 
90	  	      UPDATE SET T.DInd = 'N', T.updated_date = S.UpdatedDate, T.DelUserID = NULL, T.DelDate = NULL, T.ApproveDelUserID = NULL, T.ApproveDelDate = NULL,
91	  	            T.DelDate_UTC = NULL, T.EnterFlag = 'D', T.updated_date_UTC = GETUTCDATE(),
92	  	            T.ADDRESS = S.ADDRESS, T.STATE = ISNULL(S.City,''), T.COUNTRY = S.Country, T.IDENT = S.IdentType, T.IDNUM = S.IdentNumber, T.DOB = S.DOB, T.Gender = S.Gender
93	  	   OUTPUT CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ent_num ELSE Deleted.ent_num END,
94	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.spec_name ELSE Deleted.spec_name END,
95	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.updated_date ELSE Deleted.updated_date END,
96	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.remark ELSE Deleted.remark END,
97	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.DInd ELSE Deleted.DInd END,
98	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.creUserID ELSE Deleted.creUserID END,
99	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.IDENTO ELSE Deleted.IDENTO END,
100	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.STATE ELSE Deleted.STATE END,
101	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ADDRESS ELSE Deleted.ADDRESS END,
102	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.COUNTRY ELSE Deleted.COUNTRY END,
103	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.GROUPID ELSE Deleted.GROUPID END,
104	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.IDENT ELSE Deleted.IDENT END,
105	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.IDNUM ELSE Deleted.IDNUM END,
106	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.DOB ELSE Deleted.DOB END,
107	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.Gender ELSE Deleted.Gender END,
108	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.EnterFlag ELSE Deleted.EnterFlag END,
109	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ApprovedBy ELSE Deleted.ApprovedBy END,
110	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ApprovedDate ELSE Deleted.ApprovedDate END,
111	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.DelUserID ELSE Deleted.DelUserID END,
112	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.DelDate ELSE Deleted.DelDate END,
113	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ApproveDelUserID ELSE Deleted.ApproveDelUserID END,
114	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ApproveDelDate ELSE Deleted.ApproveDelDate END,
115	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.creDate ELSE Deleted.creDate END,
116	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.updated_date_UTC ELSE Deleted.updated_date_UTC END,
117	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ApprovedDate_UTC ELSE Deleted.ApprovedDate_UTC END,
118	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.DelDate_UTC ELSE Deleted.DelDate_UTC END,
119	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ApproveDelDate_UTC ELSE Deleted.ApproveDelDate_UTC END,
120	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.creDate_UTC ELSE Deleted.creDate_UTC END,
121	  	      CASE WHEN $ACTION = 'INSERT' OR ($ACTION = 'UPDATE' AND Deleted.DInd = 'Y' AND Inserted.DInd = 'N') THEN '01' WHEN $ACTION = 'UPDATE' AND Inserted.DInd = 'Y' THEN '03' ELSE '02' END, 
122	  	      GETUTCDATE()
123	  	   INTO BSADB.dbo.OFAC_GLSpec_Log (ent_num, spec_name, updated_date, remark, DInd, creUserID, IDENTO, STATE, ADDRESS, COUNTRY, GROUPID, IDENT, IDNUM, DOB,Gender,
124	  	                EnterFlag, ApprovedBy, ApprovedDate, DelUserID, DelDate, ApproveDelUserID, ApproveDelDate, CreationDate, updated_date_UTC,
125	  	                ApprovedDate_UTC, DelDate_UTC, ApproveDelDate_UTC, CreationDate_UTC, ActionCD, ActionDt);
126	  	
127	  	   --20251219 IT20202509021049 split Sanctions Control & Ownership list from dowjones
128	  	   IF OBJECT_ID('tempdb..#tempDJBlacklistSCO') IS NOT NULL
129	  	      DROP TABLE #tempDJBlacklistSCO
130	  	   IF OBJECT_ID('tempdb..#tempDJBlacklistDetailsSCO') IS NOT NULL
131	  	      DROP TABLE #tempDJBlacklistDetailsSCO
132	  	
133	  	   CREATE TABLE #tempDJBlacklistSCO (EntNum INT NOT NULL, SdnName NVARCHAR(350) NOT NULL, UpdatedDate VARCHAR(8) NOT NULL)
134	  	
135	  	   CREATE TABLE #tempDJBlacklistDetailsSCO(Num INT ,EntNum INT NOT NULL, SdnName NVARCHAR(350) NOT NULL, UpdatedDate VARCHAR(8) NOT NULL, 
136	  	         Address NVARCHAR(400), City NVARCHAR(200), Country NVARCHAR(100), IdentType VARCHAR(2), IdentNumber NVARCHAR(44), DOB VARCHAR(8))
137	  	        
138	  	   INSERT INTO #tempDJBlacklistSCO (EntNum, SdnName, UpdatedDate )
139	  	   SELECT  C.EntNum, C.SdnName, MAX(C.UpdatedDate) AS UpdatedDate 
140	  	   from BSADB.dbo.DJ_Entity A JOIN BSADB.dbo.DJ_ListCategory B ON A.EntityId = B.EntityId
141	  	   JOIN DJ_List_Data_A C on a.EntityId = C.EntNum
142	  	   LEFT JOIN DJ_AltName D ON C.EntNum = D.EntityId AND C.AltNum = D.AltNameId
143	  	   WHERE A.PersonStatus <> 'Inactive' and B.Description3 IN (
144	  	   'OFAC Related - Majority Owned',
145	  	   'OFAC Related – Control',
146	  	   'OFAC - Regional Sanctions Related - Majority Owned',
147	  	   'OFAC - Regional Sanctions Related - Control',
148	  	   'EU Related - Majority Owned',
149	  	   'EU Related - Control',
150	  	   'EU - Regional Sanctions Related - Majority Owned',
151	  	   'EU - Regional Sanctions Related – Control'
152	  	   ) 
153	  	   AND ISNULL(D.NameSubType,'') <> 'OSN'
154	  	   AND B.Deleted = 0
155	  	   GROUP BY C.EntNum, C.SdnName 
156	  	
157	  	   IF OBJECT_ID('tempdb..#DJ_SanctionListReference') IS NOT NULL
158	  	       DROP TABLE #DJ_SanctionListReference
159	  	
160	  	   SELECT  T.EntityId,
161	  	        STUFF((
162	  	            SELECT ';' + t2.SanctionListReference
163	  	            FROM DJ_SanctionListReference t2
164	  	            WHERE T2.EntityId = T.EntityId
165	  	            FOR XML PATH(''), TYPE
166	  	        ).value('.', 'nvarchar(max)'), 1, 1, '') AS SanctionListReference
167	  	   INTO #DJ_SanctionListReference
168	  	   FROM    DJ_SanctionListReference t
169	  	   GROUP BY T.EntityId;
170	  	   
171	  	   UPDATE #DJ_SanctionListReference
172	  	   SET SanctionListReference = 'Visit the Dow Jones for further information'
173	  	   WHERE len(SanctionListReference) >= 400
174	  	
175	  	   ;WITH TempAddress AS (
176	  	      SELECT A.EntityId, A.SanctionListReference, ROW_NUMBER() OVER(PARTITION BY A.EntityId ORDER BY A.EntityId) 'RowId'
177	  	      FROM #DJ_SanctionListReference A WITH(NOLOCK) 
178	  	      WHERE EXISTS(SELECT 1 FROM #tempDJBlacklistSCO X WHERE A.EntityId = X.EntNum)
179	  	   )/*, TempIdent AS(
180	  	      SELECT EntityId, CASE WHEN ISNULL(IdentificationType,'') = '' THEN NULL ELSE @OtherIdentMthCode END 'IdentificationType', IdentificationNumber, ROW_NUMBER() OVER(PARTITION BY A.EntityId ORDER BY A.Id) 'RowId' 
181	  	      FROM BSADB.dbo.DJ_IdentificationTypeInfo A WITH(NOLOCK) WHERE A.Deleted = 0 AND EXISTS(SELECT 1 FROM #tempDJBlacklistSCO X WHERE A.EntityId = X.EntNum)
182	  	   ), TempDOB AS (
183	  	      SELECT EntityId, InfoDay, InfoMonth, InfoYear, ROW_NUMBER() OVER(PARTITION BY A.EntityId ORDER BY A.Id) 'RowId' 
184	  	      FROM BSADB.dbo.DJ_DateTypeInfo A WITH(NOLOCK) WHERE A.DateType = 'Date of Birth' AND A.Deleted = 0 AND EXISTS(SELECT 1 FROM #tempDJBlacklistSCO X WHERE A.EntityId = X.EntNum)
185	  	   )*/, TempNUM AS (
186	  	      SELECT A.spec_name, A.updated_date, A.remark, A.TaxID, A.IDENT, A.IDENTO, A.TaxIDCD, A.IDNUM, A.issBy, A.DInd, A.creUserID, A.STATE, A.ADDRESS, A.COUNTRY,
187	  	      A.GROUPID, A.SOURCE, A.EnterFlag, A.ApprovedBy, A.ApprovedDate, A.DelUserID, A.DelDate, A.ApproveDelUserID, A.ApproveDelDate, A.creDate,
188	  	      A.updated_date_UTC, A.ApprovedDate_UTC, A.DelDate_UTC, A.ApproveDelDate_UTC, A.creDate_UTC, A.ent_num, A.DOB
189	  	      FROM BSADBTW..OFAC_SPEC A WHERE A.GROUPID = 27 and A.DInd = 'N'
190	  	   )
191	  	   INSERT INTO #tempDJBlacklistDetailsSCO (Num, EntNum, SdnName, UpdatedDate, Address)
192	  	   SELECT E.ent_num, A.EntNum, A.SdnName, A.UpdatedDate, B.SanctionListReference
193	  	   FROM #tempDJBlacklistSCO A 
194	  	      LEFT JOIN TempAddress B ON A.EntNum = B.EntityId AND B.RowId = 1 
195	  	      LEFT JOIN TempNUM E on A.EntNum = E.remark and A.SdnName = E.spec_name
196	  	
197	  	   DECLARE @MAXNo INT
198	  	   SELECT @MAXNo=MAX(ent_num) FROM BSADBTW..OFAC_SPEC
199	  	
200	  	   UPDATE #tempDJBlacklistDetailsSCO
201	  	   SET @MAXNo = @MAXNo +1 , Num = @MAXNo
202	  	   WHERE Num IS NULL
203	  	
204	  	   ;WITH TargetTable AS (
205	  	      SELECT A.spec_name, A.updated_date, A.remark, A.TaxID, A.IDENT, A.IDENTO, A.TaxIDCD, A.IDNUM, A.issBy, A.DInd, A.creUserID, A.STATE, A.ADDRESS, A.COUNTRY,
206	  	            A.GROUPID, A.SOURCE, A.EnterFlag, A.ApprovedBy, A.ApprovedDate, A.DelUserID, A.DelDate, A.ApproveDelUserID, A.ApproveDelDate, A.creDate,
207	  	            A.updated_date_UTC, A.ApprovedDate_UTC, A.DelDate_UTC, A.ApproveDelDate_UTC, A.creDate_UTC, A.ent_num, A.DOB
208	  	      FROM BSADBTW..OFAC_SPEC A WHERE A.GROUPID = 27
209	  	   )
210	  	   MERGE TargetTable T
211	  	   USING (SELECT NUM, EntNum, SdnName, UpdatedDate, Address, City, Country, IdentType, IdentNumber, DOB FROM #tempDJBlacklistDetailsSCO) S
212	  	   ON T.spec_name = S.SdnName AND T.remark = CAST(S.EntNum AS VARCHAR(100))
213	  	   WHEN NOT MATCHED BY TARGET THEN
214	  	      INSERT (spec_name,updated_date,remark,TaxID,IDENT,IDENTO,TaxIDCD,IDNUM,IssBy,DInd,
215	  	            creUserID,STATE,ADDRESS,COUNTRY,GROUPID,SOURCE,EnterFlag,ApprovedBy,ApprovedDate,DelUserID,
216	  	            DelDate,ApproveDelUserID,ApproveDelDate,creDate,updated_date_UTC,ApprovedDate_UTC,DelDate_UTC,ApproveDelDate_UTC,creDate_UTC,
217	  	            ent_num,DOB)
218	  	      VALUES(S.SdnName, S.UpdatedDate,EntNum,'', S.IdentType, S.IdentNumber,'','','' ,'N', 
219	  	            'System Admin', ISNULL(S.City,''), S.Address, S.Country, '27', 'Sanctions Control Ownership', 'D', 'System Admin', GETDATE(), NULL, 
=======
 	!>	            NULL, NULL, NULL, GETDATE(),GETUTCDATE(),GETUTCDATE(),NULL,NULL,GETUTCDATE(),S.num,
 	!>	            S.DOB)
 	!>	   WHEN NOT MATCHED BY SOURCE AND T.DInd = 'N' THEN 
 	!>	      UPDATE SET T.DInd = 'Y', T.DelUserID = 'System Admin', T.DelDate = GETDATE(), T.ApproveDelUserID = 'System Admin', T.ApproveDelDate = GETDATE(),
 	!>	            T.DelDate_UTC = GETUTCDATE()
 	!>	   WHEN MATCHED AND (T.DInd = 'Y' OR S.UpdatedDate <> T.updated_date) THEN 
 	!>	      UPDATE SET T.DInd = 'N', T.updated_date = S.UpdatedDate, T.DelUserID = NULL, T.DelDate = NULL, T.ApproveDelUserID = NULL, T.ApproveDelDate = NULL,
 	!>	            T.DelDate_UTC = NULL, T.EnterFlag = 'D', T.updated_date_UTC = GETUTCDATE(),
 	!>	            T.ADDRESS = S.ADDRESS, T.STATE = ISNULL(S.City,''), T.COUNTRY = S.Country, T.IDENT = S.IdentType, T.IDNUM = S.IdentNumber, T.DOB = S.DOB
 	!>	   OUTPUT CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ent_num ELSE Deleted.ent_num END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.spec_name ELSE Deleted.spec_name END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.updated_date ELSE Deleted.updated_date END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.remark ELSE Deleted.remark END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.taxid ELSE Deleted.taxid END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.IDENT ELSE Deleted.IDENT END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.IDENTO ELSE Deleted.IDENTO END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.TaxIDCD ELSE Deleted.TaxIDCD END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.IDNUM ELSE Deleted.IDNUM END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.issby ELSE Deleted.issby END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.DInd ELSE Deleted.DInd END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.creUserID ELSE Deleted.creUserID END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN 'System Admin' ELSE 'System Admin' END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN GETUTCDATE() ELSE GETUTCDATE() END,      
 	!>	      CASE WHEN $ACTION = 'INSERT' OR ($ACTION = 'UPDATE' AND Deleted.DInd = 'Y' AND Inserted.DInd = 'N') THEN '01' WHEN $ACTION = 'UPDATE' AND Inserted.DInd = 'Y' THEN '03' ELSE '02' END, 
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ADDRESS ELSE Deleted.ADDRESS END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.STATE ELSE Deleted.STATE END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.COUNTRY ELSE Deleted.COUNTRY END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.GROUPID ELSE Deleted.GROUPID END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.SOURCE ELSE Deleted.SOURCE END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.EnterFlag ELSE Deleted.EnterFlag END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ApprovedBy ELSE Deleted.ApprovedBy END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ApprovedDate ELSE Deleted.ApprovedDate END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.DelUserID ELSE Deleted.DelUserID END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.DelDate ELSE Deleted.DelDate END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ApproveDelUserID ELSE Deleted.ApproveDelUserID END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ApproveDelDate ELSE Deleted.ApproveDelDate END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN GETUTCDATE() ELSE GETUTCDATE() END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ApprovedDate_UTC ELSE Deleted.ApprovedDate_UTC END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.DelDate_UTC ELSE Deleted.DelDate_UTC END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ApproveDelDate_UTC ELSE Deleted.ApproveDelDate_UTC END,
 	!>	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.DOB ELSE Deleted.DOB END
 	!>	   INTO BSADBTW.dbo.OFAC_SPEC_LOG (ent_num, spec_name, updated_date, remark, TaxID, IDENT, IDENTO,
 	!>	      TaxIDCD, IDNUM, IssBy, DInd, creUserID, UserID, ActionDT, ActionCD, Address, State, Country, GROUPID, SOURCE, EnterFlag,
 	!>	      ApprovedBy, ApprovedDate, DelUserID, DelDate, ApproveDelUserID, ApproveDelDate, ActionDT_UTC, ApprovedDate_UTC, DelDate_UTC, ApproveDelDate_UTC ,
 	!>	      DOB);
=======
	  	
=======
 	!>	   --20260512 IT20202509031043 split Sanctions IDs list from dowjones
 	!>	   IF OBJECT_ID('tempdb..#tempDJBlacklistSID') IS NOT NULL
 	!>	      DROP TABLE #tempDJBlacklistSID
 	!>	   IF OBJECT_ID('tempdb..#tempDJBlacklistDetailsSID') IS NOT NULL
 	!>	      DROP TABLE #tempDJBlacklistDetailsSID
=======
	  	
=======
 	!>	   CREATE TABLE #tempDJBlacklistSID (EntNum INT NOT NULL, IdentificationType NVARCHAR(100) NOT NULL, IdentificationNumber NVARCHAR(350) NOT NULL, UpdatedDate VARCHAR(8) NOT NULL)
=======
	  	
=======
 	!>	   CREATE TABLE #tempDJBlacklistDetailsSID(Num INT ,EntNum INT NOT NULL, IdentificationNumber NVARCHAR(350) NOT NULL, IdentificationType NVARCHAR(350) NOT NULL, UpdatedDate VARCHAR(8) NOT NULL, 
 	!>	         Address NVARCHAR(400), City NVARCHAR(200), Country NVARCHAR(100), IdentType VARCHAR(2), IdentNumber NVARCHAR(44), DOB VARCHAR(8))
=======
	  	
=======
 	!>	   INSERT INTO #tempDJBlacklistSID (EntNum, IdentificationType, IdentificationNumber, UpdatedDate )
 	!>	   SELECT  A.EntityId, D.IdentificationType, D.IdentificationNumber,  MAX(C.UpdatedDate) AS UpdatedDate 
 	!>	   from BSADB.dbo.DJ_Entity A JOIN BSADB.dbo.DJ_ListCategory B ON A.EntityId = B.EntityId
 	!>	   JOIN BSADB.dbo.DJ_List_Data_A C on a.EntityId = C.EntNum
 	!>	   LEFT JOIN BSADB.dbo.DJ_IdentificationTypeInfo D ON A.EntityId = D.EntityId
 	!>	   WHERE A.PersonStatus <> 'Inactive' and (B.Description2  = 'Sanctions Lists' or B.Description3 IN (
 	!>	   'OFAC Related - Majority Owned',
 	!>	   'OFAC Related – Control',
 	!>	   'OFAC - Regional Sanctions Related - Majority Owned',
 	!>	   'OFAC - Regional Sanctions Related - Control',
 	!>	   'EU Related - Majority Owned',
 	!>	   'EU Related - Control',
 	!>	   'EU - Regional Sanctions Related - Majority Owned',
 	!>	   'EU - Regional Sanctions Related – Control'
 	!>	   )) AND B.Deleted = 0
 	!>	   AND D.IdentificationType in 
 	!>	   ('Aircraft Construction, Line, Fleet or Serial Number',
 	!>	   'Aircraft Manufacturer''s Serial Number (MSN)',
 	!>	   'Bank Identifier Code (BIC)',
 	!>	   'Central Registration Depository (CRD)',
 	!>	   'Company Identification No.',
 	!>	   'Driving Licence No.',
 	!>	   'DUNS Number',
 	!>	   'International Maritime Organization (IMO) Ship No.',
 	!>	   'International Securities Identification Number (ISIN)',
 	!>	   'Legal Entity Identifier (LEI)',
 	!>	   'National ID',
 	!>	   'National Provider Identifier (NPI)',
 	!>	   'National Tax No.',
 	!>	   'Others',
 	!>	   'Passport No.',
 	!>	   'Social Security No.',
 	!>	   'Email',
 	!>	   'Fax',
 	!>	   'Phone') AND D.Deleted = 0
 	!>	   GROUP BY A.EntityId,D.IdentificationType, D.IdentificationNumber
=======
	  	
=======
 	!>	   ;WITH TempNUM AS (
 	!>	      SELECT A.spec_name, A.updated_date, A.remark, A.TaxID, A.IDENT, A.IDENTO, A.TaxIDCD, A.IDNUM, A.issBy, A.DInd, A.creUserID, A.STATE, A.ADDRESS, A.COUNTRY,
 	!>	      A.GROUPID, A.SOURCE, A.EnterFlag, A.ApprovedBy, A.ApprovedDate, A.DelUserID, A.DelDate, A.ApproveDelUserID, A.ApproveDelDate, A.creDate,
 	!>	      A.updated_date_UTC, A.ApprovedDate_UTC, A.DelDate_UTC, A.ApproveDelDate_UTC, A.creDate_UTC, A.ent_num, A.DOB, REPLACE(A.ADDRESS,'; Please go to Dowjones website for more detail','') AS IdentificationType
 	!>	      FROM BSADBTW..OFAC_SPEC A WHERE A.GROUPID = 29 and A.DInd = 'N'
 	!>	   )
 	!>	   INSERT INTO #tempDJBlacklistDetailsSID (Num, EntNum, IdentificationNumber, IdentificationType, UpdatedDate, Address)
 	!>	   SELECT E.ent_num, A.EntNum, A.IdentificationNumber, A.IdentificationType, A.UpdatedDate, A.IdentificationType + '; Please go to Dowjones website for more detail'
 	!>	   FROM #tempDJBlacklistSID A 
 	!>	      LEFT JOIN TempNUM E on A.EntNum = E.remark and A.IdentificationNumber = E.spec_name and A.IdentificationType = E.IdentificationType
=======
	  	
=======
 	!>	   
 	!>	   SELECT @MAXNo=MAX(ent_num) FROM BSADBTW..OFAC_SPEC
=======
	  	
=======
 	!>	   UPDATE #tempDJBlacklistDetailsSID
 	!>	   SET @MAXNo = @MAXNo +1 , Num = @MAXNo
 	!>	   WHERE Num IS NULL
=======
	  	
=======
 	!>	   ;WITH TargetTable AS (
 	!>	      SELECT A.spec_name, A.updated_date, A.remark, A.TaxID, A.IDENT, A.IDENTO, A.TaxIDCD, A.IDNUM, A.issBy, A.DInd, A.creUserID, A.STATE, A.ADDRESS, A.COUNTRY,
 	!>	            A.GROUPID, A.SOURCE, A.EnterFlag, A.ApprovedBy, A.ApprovedDate, A.DelUserID, A.DelDate, A.ApproveDelUserID, A.ApproveDelDate, A.creDate,
 	!>	            A.updated_date_UTC, A.ApprovedDate_UTC, A.DelDate_UTC, A.ApproveDelDate_UTC, A.creDate_UTC, A.ent_num, A.DOB, REPLACE(A.ADDRESS,'; Please go to Dowjones website for more detail','') AS IdentificationType
 	!>	      FROM BSADBTW..OFAC_SPEC A WHERE A.GROUPID = 29
 	!>	   )
 	!>	   MERGE TargetTable T
 	!>	   USING (SELECT NUM, EntNum, IdentificationNumber, IdentificationType, UpdatedDate, Address, City, Country, IdentType, IdentNumber, DOB FROM #tempDJBlacklistDetailsSID) S
 	!>	   ON T.spec_name = S.IdentificationNumber AND T.remark = CAST(S.EntNum AS VARCHAR(100)) AND T.IdentificationType = S.IdentificationType
 	!>	   WHEN NOT MATCHED BY TARGET THEN
 	!>	      INSERT (spec_name,updated_date,remark,TaxID,IDENT,IDENTO,TaxIDCD,IDNUM,IssBy,DInd,
 	!>	            creUserID,STATE,ADDRESS,COUNTRY,GROUPID,SOURCE,EnterFlag,ApprovedBy,ApprovedDate,DelUserID,
 	!>	            DelDate,ApproveDelUserID,ApproveDelDate,creDate,updated_date_UTC,ApprovedDate_UTC,DelDate_UTC,ApproveDelDate_UTC,creDate_UTC,
 	!>	            ent_num,DOB)
 	!>	      VALUES(S.IdentificationNumber, S.UpdatedDate,EntNum,'', S.IdentType, S.IdentNumber,'','','' ,'N', 
 	!>	            'System Admin', ISNULL(S.City,''), S.Address, S.Country, '29', 'Sanctions IDs', 'D', 'System Admin', GETDATE(), NULL, 
=======
220	  	            NULL, NULL, NULL, GETDATE(),GETUTCDATE(),GETUTCDATE(),NULL,NULL,GETUTCDATE(),S.num,
221	  	            S.DOB)
222	  	   WHEN NOT MATCHED BY SOURCE AND T.DInd = 'N' THEN 
223	  	      UPDATE SET T.DInd = 'Y', T.DelUserID = 'System Admin', T.DelDate = GETDATE(), T.ApproveDelUserID = 'System Admin', T.ApproveDelDate = GETDATE(),
224	  	            T.DelDate_UTC = GETUTCDATE()
225	  	   WHEN MATCHED AND (T.DInd = 'Y' OR S.UpdatedDate <> T.updated_date) THEN 
226	  	      UPDATE SET T.DInd = 'N', T.updated_date = S.UpdatedDate, T.DelUserID = NULL, T.DelDate = NULL, T.ApproveDelUserID = NULL, T.ApproveDelDate = NULL,
227	  	            T.DelDate_UTC = NULL, T.EnterFlag = 'D', T.updated_date_UTC = GETUTCDATE(),
228	  	            T.ADDRESS = S.ADDRESS, T.STATE = ISNULL(S.City,''), T.COUNTRY = S.Country, T.IDENT = S.IdentType, T.IDNUM = S.IdentNumber, T.DOB = S.DOB
229	  	   OUTPUT CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ent_num ELSE Deleted.ent_num END,
230	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.spec_name ELSE Deleted.spec_name END,
231	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.updated_date ELSE Deleted.updated_date END,
232	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.remark ELSE Deleted.remark END,
233	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.taxid ELSE Deleted.taxid END,
234	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.IDENT ELSE Deleted.IDENT END,
235	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.IDENTO ELSE Deleted.IDENTO END,
236	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.TaxIDCD ELSE Deleted.TaxIDCD END,
237	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.IDNUM ELSE Deleted.IDNUM END,
238	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.issby ELSE Deleted.issby END,
239	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.DInd ELSE Deleted.DInd END,
240	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.creUserID ELSE Deleted.creUserID END,
241	  	      CASE WHEN $ACTION IN ('INSERT') THEN 'System Admin' ELSE 'System Admin' END,
242	  	      CASE WHEN $ACTION IN ('INSERT') THEN GETUTCDATE() ELSE GETUTCDATE() END,      
243	  	      CASE WHEN $ACTION = 'INSERT' OR ($ACTION = 'UPDATE' AND Deleted.DInd = 'Y' AND Inserted.DInd = 'N') THEN '01' WHEN $ACTION = 'UPDATE' AND Inserted.DInd = 'Y' THEN '03' ELSE '02' END, 
244	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ADDRESS ELSE Deleted.ADDRESS END,
245	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.STATE ELSE Deleted.STATE END,
246	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.COUNTRY ELSE Deleted.COUNTRY END,
247	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.GROUPID ELSE Deleted.GROUPID END,
248	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.SOURCE ELSE Deleted.SOURCE END,
249	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.EnterFlag ELSE Deleted.EnterFlag END,
250	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ApprovedBy ELSE Deleted.ApprovedBy END,
251	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ApprovedDate ELSE Deleted.ApprovedDate END,
252	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.DelUserID ELSE Deleted.DelUserID END,
253	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.DelDate ELSE Deleted.DelDate END,
254	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ApproveDelUserID ELSE Deleted.ApproveDelUserID END,
255	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ApproveDelDate ELSE Deleted.ApproveDelDate END,
256	  	      CASE WHEN $ACTION IN ('INSERT') THEN GETUTCDATE() ELSE GETUTCDATE() END,
257	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ApprovedDate_UTC ELSE Deleted.ApprovedDate_UTC END,
258	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.DelDate_UTC ELSE Deleted.DelDate_UTC END,
259	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.ApproveDelDate_UTC ELSE Deleted.ApproveDelDate_UTC END,
260	  	      CASE WHEN $ACTION IN ('INSERT') THEN Inserted.DOB ELSE Deleted.DOB END
261	  	   INTO BSADBTW.dbo.OFAC_SPEC_LOG (ent_num, spec_name, updated_date, remark, TaxID, IDENT, IDENTO,
262	  	      TaxIDCD, IDNUM, IssBy, DInd, creUserID, UserID, ActionDT, ActionCD, Address, State, Country, GROUPID, SOURCE, EnterFlag,
263	  	      ApprovedBy, ApprovedDate, DelUserID, DelDate, ApproveDelUserID, ApproveDelDate, ActionDT_UTC, ApprovedDate_UTC, DelDate_UTC, ApproveDelDate_UTC ,
264	  	      DOB);
265	  	
266	  	END
267	  	
268	  	
269	  	
270	  	


這是之前台灣的diff 
我現在是要做海外的  那看起來不用
我主要是要做Sansaction ID
