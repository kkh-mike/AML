這段 SQL 程式碼主要是透過 MERGE 語法，將整理好的道瓊（Dow Jones）制裁名單身分識別資料（Sanctions IDs），比對並寫入到目標資料表 BSADBTW..OFAC_SPEC（且同步記錄異動到 OFAC_SPEC_LOG 歷史日誌表）。
它是根據以下三個核心維度進行比對（Join 條件），來決定要寫入、更新還是刪除：
1. 比對的 key（核心防重基準）
在 MERGE 的 ON 條件中，兩張表是透過這三個欄位來進行比對的：
 * 證件號碼：T.spec_name (目標) = S.IdentificationNumber (來源)
 * 道瓊實體編號：T.remark (目標) = S.EntNum (來源，轉成字串)
 * 證件類型：T.IdentificationType (目標) = S.IdentificationType (來源) （註：目標表的證件類型是從 ADDRESS 欄位中去除了特定網址字串後還原出來的）
2. 資料寫入與異動的三大情境（寫入邏輯）
當這三個 Key 比對之後，程式會根據以下三種情況進行「寫入」或「修改」：
❶ 新增（WHEN NOT MATCHED BY TARGET）
 * 條件：如果道瓊來源檔（S）有這筆證件資料，但目標表（T）完全找不到相同的 Key。
 * 動作：直接 INSERT 新增一筆資料。
   * 預設狀態為有效（DInd = 'N'）。
   * 流水號 ent_num 會自動帶入計算後的 @MAXNo + 1。
   * 建立者與審核者皆寫入 'System Admin'。
❷ 軟刪除 / 停用（WHEN NOT MATCHED BY SOURCE）
 * 條件：如果目標表（T）有一筆狀態為有效（DInd = 'N'）的資料，但在這次的道瓊來源檔（S）中已經消失了（代表該證件已被道瓊移除或失效）。
 * 動作：進行 UPDATE 將其軟刪除。
   * 將狀態改為停用（DInd = 'Y'）。
   * 寫入刪除者（DelUserID = 'System Admin'）與刪除時間（DelDate = GETDATE()）。
❸ 啟用或更新（WHEN MATCHED）
 * 條件：如果兩邊 Key 互相對得上，但符合以下任一條件：
   * 該筆資料在目標表中原本是停用狀態（T.DInd = 'Y'）。
   * 道瓊來源檔的更新日期與目標表不一致（S.UpdatedDate <> T.updated_date）。
 * 動作：進行 UPDATE 更新資料。
   * 將狀態重新啟用（DInd = 'N'），並清空原本的刪除人員與時間。
   * 更新最新的 updated_date、證件地址（ADDRESS）、國家（COUNTRY）、城市（STATE）與出生日期（DOB）等欄位。
3. 同步寫入 Log 紀錄
在 MERGE 的最下方有一個 OUTPUT ... INTO BSADBTW.dbo.OFAC_SPEC_LOG。
只要上述任何一個動作（新增、修改、刪除）觸發，SQL 就會根據 $ACTION（INSERT 或 UPDATE）以及 DInd 的狀態變化，產生相對應的行為代碼（ActionCD）：
 * 01 (新增/重新啟用)：當全新寫入資料，或原本停用（Y）的資料被重新啟用（N）時。
 * 03 (停用/刪除)：當資料被標記為 DInd = 'Y' 時。
 * 02 (一般更新)：一般的欄位資訊異動時。
這些異動紀錄都會被實時寫入到 OFAC_SPEC_LOG 中，供日後審計稽核。
