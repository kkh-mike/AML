從 Error 來看：

字串或二進位資料會被截斷(String or binary data would be truncated)

* Procedure：DataMigrator_Integrate
* Line：3883
* 發生點：EXEC sp_executesql @sql

這代表 SQL Dynamic SQL 執行的時候，有欄位長度不夠。

但是這種 Error 最大的問題是：

真正出錯的 SQL 在 @sql 裡面，所以 Line 3883 不一定是錯的地方。

因此有幾個地方要查。

第一個嫌疑犯

INSERT INTO Customer_Migrate_Exception_Table

如果第一次建立 Table 是

SELECT ....
INTO Customer_Migrate_Exception_Table

之後又

INSERT INTO Customer_Migrate_Exception_Table

就有可能發生。

例如：

第一次
CustomerNo
------------
1234567890
varchar(10)
第二次
CustomerNo
------------
123456789012345

就爆了。

⸻

第二個嫌疑犯

MERGE BSA_SUB_CUST_FACT

例如

SOURCE.CustomerNo

最後會變成

TARGET.CustomerNo

如果

BSA_SUB_CUST_FACT
CustomerNo
------------
varchar(20)

但是

TS_CUST_ID
----------
1234567890123456789012345
25碼

也會炸。

尤其這段：

COALESCE(
    B.TargetColumn,
    A.CustomerNo
)

跟

COALESCE(
    s.TS_CUST_ID,
    B.CustomerNo
)

都有可能。

⸻

第三個嫌疑犯

這個比較值得懷疑。

Customer_Migrate_Exception_Table

是這樣建立的：

SELECT
Y.xxx,
Y.xxx,
'Excluded in merging' AS Reason,
A.*
INTO ....

注意：

Reason
---------
Excluded in merging

SQL Server會自動推：

varchar(21)

但是如果以前的 Table 已經存在：

Reason
-------
varchar(10)

之後

INSERT
'Excluded in merging'

就會爆了。

所以可以先看一下：

sp_help Customer_Migrate_Exception_Table

或

SELECT
    COLUMN_NAME,
    DATA_TYPE,
    CHARACTER_MAXIMUM_LENGTH
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_NAME =
'Customer_Migrate_Exception_Table'

看看是不是某個欄位長度太短。

⸻

第四個嫌疑犯

Alias_Name

因為這段只 insert 了部分欄位：

INSERT(
    CIF_KEY,
    CustomerNo,
    Alias_Name,
    ....
)
VALUES(
    NEWID(),
    SOURCE.CustomerNo,
    SOURCE.Alias_Name,
    ....
)

如果：

Customer_Migrate_buffer
Alias_Name
----------
varchar(500)

但是

BSA_SUB_CUST_FACT
Alias_Name
---------
varchar(100)

也會炸。

建議直接比一下：

sp_help Customer_Migrate_buffer
sp_help BSA_SUB_CUST_FACT
sp_help Customer_Migrate_Exception_Table

確認欄位長度。

⸻

如何快速找到是哪個欄位？

可以先查最大的資料長度：

SELECT
MAX(LEN(CustomerNo)),
MAX(LEN(Alias_Name)),
MAX(LEN(CUST_NME)),
MAX(LEN(NATIVE_CUST_NME)),
MAX(LEN(C_CTRYCD))
FROM Customer_Migrate_buffer

再查：

sp_help BSA_SUB_CUST_FACT

例如看到：

欄位                 長度
CustomerNo          20
Alias_Name          50
CUST_NME            100
NATIVE_CUST_NME     50

如果：

MAX(LEN(Alias_Name))
=120

答案就出來了。

⸻

如果 SQL Server 是 2019+

可以把這個打開：

DBCC TRACEON(460);

或是 SQL 2019：

ALTER DATABASE SCOPED CONFIGURATION
SET VERBOSE_TRUNCATION_WARNINGS = ON;

Error 就會變成：

String or binary data would be truncated
in table 'dbo.BSA_SUB_CUST_FACT'
column 'Alias_Name'
Truncated value:
xxxxxxxxxxxxxxxxxxxxxxxx

會直接告訴你：

哪一張 Table
哪一個 Column
被截掉的是什麼值

非常好用。

⸻

我的判斷

目前最可疑的地方有兩個：

1. Customer_Migrate_Exception_Table 的 INSERT
    * 第一次 SELECT INTO 建出來的欄位長度和後續資料不一致。
2. MERGE INTO BSA_SUB_CUST_FACT
    * CustomerNo
    * Alias_Name
    * CUST_NME
    * NATIVE_CUST_NME
    * Mapping 後的 TS_CUST_ID
    * Cust_Method_Of_Opening
    * Cust_Product_Service
    * Cust_Account_Purpose

由於錯誤發生在 EXEC sp_executesql @sql，它只能告訴你「動態 SQL 裡有資料被截斷」，不能直接指出欄位。因此最有效的排查方式是：

1. 先 PRINT @sql 或 SELECT @sql 看 Line 3883 執行的是哪一段 Dynamic SQL。
2. sp_help Customer_Migrate_Exception_Table、sp_help BSA_SUB_CUST_FACT 檢查欄位長度。
3. 用 MAX(LEN(...)) 檢查 Customer_Migrate_buffer 的實際資料長度。
4. 如果環境允許，開啟 VERBOSE_TRUNCATION_WARNINGS，SQL Server 會直接告訴你是哪個欄位被截斷，可以大幅縮短除錯時間。