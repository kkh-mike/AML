@

不用真的「一邊執行 .exe 一邊比對」，通常有兩種處理方式，看你方便選哪一種：
方式一：直接查 SQL 歷史紀錄（不用開 .exe，最方便）
如果這個 ETL 過去有執行過，SQL Server 的快取（Plan Cache）其實早就把語法記下來了。你不需要觸發 .exe，直接在 SSMS 開新查詢視窗執行語法即可：
SELECT TOP 50
    st.text AS [Executed_SQL_or_SP],
    qs.last_execution_time AS [Last_Executed_Time],
    qs.execution_count AS [Execution_Count]
FROM sys.dm_exec_query_stats AS qs
CROSS APPLY sys.dm_exec_sql_text(qs.sql_handle) AS st
WHERE st.text LIKE '%RAW_%'      -- 搜尋第一張圖 obods 裡的檢視表名稱
   OR st.text LIKE '%obods%'
ORDER BY qs.last_execution_time DESC;

方式二：如果一定要手動觸發 .exe（做實驗）
如果你希望 「手動跑一次 .exe 讓它去抓資料，然後即時抓出它用了哪隻 SP」：
 * 先在 SQL Server 開好 Profiler 或查詢視窗。
 * 執行 .exe 讓它開始運作。
 * 立刻執行 SQL（或者透過 SQL Server Profiler 側側監聽）：
   -- 抓過去 10 分鐘內執行的 SP 或 SQL
SELECT 
    st.text AS [SQL_Content],
    qs.last_execution_time
FROM sys.dm_exec_query_stats AS qs
CROSS APPLY sys.dm_exec_sql_text(qs.sql_handle) AS st
WHERE qs.last_execution_time >= DATEADD(MINUTE, -10, GETDATE())
ORDER BY qs.last_execution_time DESC;

💡 補充：不用跑 .exe 也能直接「靜態找出 SP」的方法
從你的截圖來看，amldbobt 資料庫裡面有很多 AC_ 開頭的 SP，而 obods 裡面有很多 RAW_ 開頭的檢視表（View）。
你直接在 amldbobt（例如選擇 BSADBHK 資料庫）貼上這段 SQL，就能直接把「程式碼裡面有去 obods 撈 RAW_ 資料」的所有 SP 一次列出來，連 .exe 都不用執行：
USE [BSADBHK]; -- 請切換到你 amldbobt 下的資料庫名稱

SELECT 
    OBJECT_NAME(m.object_id) AS [SP_Name],
    m.definition AS [SP_Code]
FROM sys.sql_modules m
INNER JOIN sys.objects o ON m.object_id = o.object_id
WHERE o.type = 'P' -- 只找 Stored Procedure
  AND (
       m.definition LIKE '%RAW_%'   -- 關鍵字：obods 裡的檢視表 prefix
    OR m.definition LIKE '%obods%'  -- 關鍵字：obods 資料庫名
    OR m.definition LIKE '%HKDB%'   -- 關鍵字：HKDB 資料庫名
  );

你要找的答案，大概率就在這個查詢結果產出的 SP 清單裡！你想先嘗試用靜態語法找，還是去查 SQL 執行紀錄？



呃呃呃呃呃呃呃



在 amldbobt（跨伺服器/跨資料庫）尋找哪些預存程序（Stored Procedure, SP）會用到 obods（例如 obods_test 或 HKDB）的資料，可以直接在 amldbobt 執行 SQL 語法搜尋 SP 的程式碼內容。
請在 amldbobt 上開啟 新查詢 (New Query)，並選擇對應的資料庫（例如 BSADBHK 或你想查的資料庫），執行以下幾種方法之一：
方法一：搜尋 SP 內文（最常用且直接）
透過 sys.sql_modules 與 sys.objects 模糊搜尋內文包含關鍵字（如 obods 或 HKDB）的預存程序：
SELECT 
    OBJECT_NAME(m.object_id) AS ProcedureName,
    o.type_desc AS ObjectType,
    m.definition AS CodeSnippet
FROM sys.sql_modules m
INNER JOIN sys.objects o ON m.object_id = o.object_id
WHERE o.type IN ('P', 'PC') -- P 代表 Stored Procedure
  AND (
       m.definition LIKE '%obods%' 
    OR m.definition LIKE '%HKDB%'
  );

方法二：如果跨伺服器使用了 Linked Server（連結伺服器）
如果 amldbobt 和 obods_test 是兩台不同的 SQL Server 執行個體，程式碼中通常會使用 四段式名稱（例如 [obods_test].[HKDB].[dbo].[TableName]）或 Linked Server 名稱。
你可以把 Linked Server 的名稱（或 IP / 機器名）加入搜尋語法：
SELECT 
    OBJECT_NAME(m.object_id) AS ProcedureName,
    m.definition
FROM sys.sql_modules m
WHERE m.definition LIKE '%<你的LinkedServer名稱或IP>%'
   OR m.definition LIKE '%obods%';

方法三：透過系統檢視表 syscomments（舊版相容）
如果伺服器版本較舊（雖然畫面上是 SQL 2016），也可以用傳統的 syscomments 搜尋：
SELECT DISTINCT 
    o.name AS ProcedureName
FROM syscomments c
INNER JOIN sys.objects o ON c.id = o.object_id
WHERE o.type = 'P'
  AND (c.text LIKE '%obods%' OR c.text LIKE '%HKDB%');

💡 小提醒與排錯建議：
 * 確定查詢執行的資料庫：畫面上 amldbobt 底下有 BSADB、BSADBAU、BSADBHK 等多個資料庫。請確認下拉選單切換到了你想查詢的資料庫（例如 BSADBHK），或加上 USE [資料庫名稱];。
 * 同義字 (Synonym) 或動態 SQL (Dynamic SQL)：
   * 如果 SP 是透過 同義字 (Synonym) 連到 obods，語法中的 LIKE '%obods%' 也能查到建立同義字的定義，或者可以直接查詢 sys.synonyms。
   * 如果程式碼是用字串拼湊後 EXEC() 或 sp_executesql 執行的動態 SQL，sys.sql_modules 依然能找得到字串裡包含的關鍵字。
