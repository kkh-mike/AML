在 amldbobt（跨伺服器/跨資料庫）尋找哪些預存程序（Stored Procedure, SP）會用到 obods（例如 obods_test 或 HKDB）的資料，可以直接在 amldbobt 執行 SQL 語法搜尋 SP 的程式碼內容。
請在 amldbobt 上開啟 新查詢 (New Query)，並選擇對應的資料庫（例如 BSADBHK 或你想查的資料庫），執行以下幾種方法之一：
方法一：搜尋 SP 內文（最常用且直接）
透過 sys.sql_modules 與 sys.objects 模糊搜尋內文包含關鍵字（如 obods 或 HKDB）的預存程序：
SELECT 
    OBJECT_NAME(m.object_id) AS ProcedureName,
    o.type_desc AS ObjectType,
    m.definition AS CodeSnippet
FROM sys.sql_modules m
INNER JOIN sys.objects o ON m.object_id = o.object_id
WHERE o.type IN ('P', 'PC') -- P 代表 Stored Procedure
  AND (
       m.definition LIKE '%obods%' 
    OR m.definition LIKE '%HKDB%'
  );

方法二：如果跨伺服器使用了 Linked Server（連結伺服器）
如果 amldbobt 和 obods_test 是兩台不同的 SQL Server 執行個體，程式碼中通常會使用 四段式名稱（例如 [obods_test].[HKDB].[dbo].[TableName]）或 Linked Server 名稱。
你可以把 Linked Server 的名稱（或 IP / 機器名）加入搜尋語法：
SELECT 
    OBJECT_NAME(m.object_id) AS ProcedureName,
    m.definition
FROM sys.sql_modules m
WHERE m.definition LIKE '%<你的LinkedServer名稱或IP>%'
   OR m.definition LIKE '%obods%';

方法三：透過系統檢視表 syscomments（舊版相容）
如果伺服器版本較舊（雖然畫面上是 SQL 2016），也可以用傳統的 syscomments 搜尋：
SELECT DISTINCT 
    o.name AS ProcedureName
FROM syscomments c
INNER JOIN sys.objects o ON c.id = o.object_id
WHERE o.type = 'P'
  AND (c.text LIKE '%obods%' OR c.text LIKE '%HKDB%');

💡 小提醒與排錯建議：
 * 確定查詢執行的資料庫：畫面上 amldbobt 底下有 BSADB、BSADBAU、BSADBHK 等多個資料庫。請確認下拉選單切換到了你想查詢的資料庫（例如 BSADBHK），或加上 USE [資料庫名稱];。
 * 同義字 (Synonym) 或動態 SQL (Dynamic SQL)：
   * 如果 SP 是透過 同義字 (Synonym) 連到 obods，語法中的 LIKE '%obods%' 也能查到建立同義字的定義，或者可以直接查詢 sys.synonyms。
   * 如果程式碼是用字串拼湊後 EXEC() 或 sp_executesql 執行的動態 SQL，sys.sql_modules 依然能找得到字串裡包含的關鍵字。
