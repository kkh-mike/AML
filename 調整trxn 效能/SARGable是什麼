**SARGable** 是英文 **S**earch **Arg**ument **Able** 的縮寫，直譯就是「具備搜尋條件能力的語法」。

用人話來說：

> **你寫的查詢條件，能讓資料庫「直接去翻目錄（走索引）」找資料；一旦破壞了它，資料庫就只能「整本書從第一頁翻到最後一頁（全表掃描）」逐筆檢查。**

---

### 一個生活比喻：圖書館找書

想像你在圖書館找書，書架上的書都是按「作者姓名」筆劃排序**放好的（這就是**索引）。

* **符合 SARGable（快）**：
你跟管理員說：「我要找**姓陳**的作者。」
* 管理員動作：直接走到「陳」字的那一區，一眼抓出所有書。（**Index Seek**，秒殺）


* **破壞 SARGable（慢）**：
你跟管理員說：「我要找作者名字**去掉第一個字以後，第二個字是『豪』**的人。」或者「我要找作者名字**前面加上『台灣作家：』之後長度是 7 個字**的人。」
* 管理員動作：崩潰。因為書本是按完整姓名排的，他不知道誰符合條件，只能**把全圖書館幾十萬本書一本一本拿下來，用筆遮掉第一個字檢查，或是加上前綴算字數**。（**Table Scan / Index Scan**，累死且極慢）



在資料庫裡，**只要你在欄位上包了函式、計算或型態轉換，資料庫就得對每一筆資料「算過一次」才能比對，索引當場報廢。**

---

### 常見的「破壞 SARGable」寫法與解法

**1. 對欄位使用函式處理（最常見的陷阱）**

* ❌ **破壞 SARGable**（全表掃描）：
```sql
-- 找 2026 年的訂單，卻把欄位包進 YEAR() 函式
WHERE YEAR(OrderDate) = 2026

```


*為什麼慢*：資料庫必須把歷史幾千萬筆訂單的 `OrderDate` 一筆筆拿出來算 `YEAR()`，才看得懂符不符合。
* **符合 SARGable**（秒翻目錄）：

```sql
-- 欄位乾乾淨淨，只比對時間範圍
WHERE OrderDate >= '2026-01-01' AND OrderDate < '2027-01-01'

```

---

**2. 在條件比對時使用字串串接或計算**

* ❌ **破壞 SARGable**：
```sql
-- 欄位本身去跟字串拼接
WHERE 'SK_' + AccountNo = 'SK_123456'
-- 或是數值運算
WHERE Price * 0.8 > 1000

```


* **符合 SARGable**：

```sql
-- 把計算移到等號另一邊，讓「資料表欄位」保持乾淨
WHERE AccountNo = '123456'
WHERE Price > 1000 / 0.8

```

---

**3. 用 `ISNULL` 或 `COALESCE` 包住欄位**

這就是你預存程序裡遇到的狀況：

* ❌ **破壞 SARGable**：
```sql
-- 為了防呆，在 JOIN 或 WHERE 條件包了 ISNULL / 字串相加
WHERE Z.ACCT_KEY = ISNULL(C.FinalAcctKey, 'SK_' + A.ACCT_KEY)

```


*為什麼慢*：目標表 `Z` 看到等號右邊不是單純的值，而是一串需要每筆動態判斷與串接的運算，優化器不敢保證它的特性，直接放棄走最高效的目錄檢索，退化成逐筆掃描。
* **符合 SARGable**：

```sql
-- 前面先處理好乾淨的值，比對時純粹「欄位 = 欄位」
WHERE Z.ACCT_KEY = C.FinalAcctKey

```

---

### 一句話記住原則

> **「條件裡，被查詢的欄位要保持乾乾淨淨，任何計算、格式轉換都留給右邊的參數去算。」**
