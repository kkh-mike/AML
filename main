SELECT ANSWER_CD, INDV_SCORE, LSTUPDATE_DATE_INDV
FROM CIP_SCENARIO_LIBRARY
WHERE TEMPLATE_ID = 'CIPT1200059'
  AND ANSWER_CD IN ('ZW', 'GN', 'PG', 'GW')  -- 舉例 Zimbabwe, Guinea 等
  AND DELETEFLAG = 'N'
ORDER BY LSTUPDATE_DATE_INDV DESC