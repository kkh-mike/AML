這張截圖把關鍵問題完全證實了：
 * DBCHK_BlackListData_Buffer 裡面有自訂名單（sdn_type = 'SPEC'），但目前只有 sdn_grp_num = 8、11 等，完全沒有 sdn_grp_num = 5（日本名單）！
 * 比對引擎在比對時，是去掃描這張 Buffer 快取表。因為 GROUPID = 5 的 72,086 筆名單根本沒有被寫入 DBCHK_BlackListData_Buffer，所以不管送檢什麼名字（包含死亡名單 Kisoni, Kambale），比對引擎都絕對抓不到。
解決與定位步驟
步驟 1：找出維護/刷新 DBCHK_BlackListData_Buffer 的 SP
在 SSMS 執行以下查詢，找出將名單寫入該 Buffer 表的 SP：
SELECT OBJECT_NAME(id) AS SP_Name
FROM syscomments
WHERE text LIKE '%DBCHK_BlackListData_Buffer%'
  AND (text LIKE '%INSERT INTO%' OR text LIKE '%MERGE%')
GROUP BY OBJECT_NAME(id);

步驟 2：檢查該 SP 為什麼漏抓 GROUPID = 5
找到 SP 名稱後（假設名稱為 DBCHK_PrepareBlackListBuffer 或類似名稱），執行：
sp_helptext '你的SP名稱';

重點檢查內容：
 * 裡面是不是有這類條件：WHERE GROUPID IN (1, 2, 8, 11)，漏加了 5？
 * 或者是串接 lowrisklistgroups 時，因為之前 GROUPID = 5 的設定被設成 NamePropertyBitCode = 0 或其他旗標，導致在寫入 Buffer 時被過濾掉了？
步驟 3：手動補跑/刷新 Buffer 測試
一旦確認並修改該前置 SP（或直接重新執行該 SP 刷新 Buffer），再查一次 Buffer 表：
SELECT COUNT(*) 
FROM BSADBJP.dbo.DBCHK_BlackListData_Buffer 
WHERE sdn_grp_num = 5;

只要這裡查出有筆數（約 7 萬多筆），接著再去送檢 Kisoni, Kambale，檢核就會立刻命中並記錄到結果表中。
