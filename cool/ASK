這張截圖非常關鍵：
 * OFAC_SPEC 裡面確實有這筆資料（ent_num = 696816, spec_name = Kisoni, Kambale, remark = 880575, GROUPID = 5, DInd = 'N'）。
 * 但是送檢 Kisoni, Kambale 時，比對引擎卻完全沒有命中。
這代表資料已經正確同步進 OFAC_SPEC，但「比對引擎（Detection Process）」在掃描黑名單時漏掉了 OFAC_SPEC 的自訂名單。
請執行以下 2 個排查動作來找出比對引擎的卡點：
步驟 1：查看比對引擎核心 SP DBCHK_ProcessDetectionResults
在 SSMS 執行以下指令查看比對邏輯：
sp_helptext 'DBCHK_ProcessDetectionResults';

重點搜尋以下關鍵字：
 * 搜尋 OFAC_SPEC：看它有沒有被 UNION 或 JOIN 到比對暫存表。
 * 搜尋 lowrisklistgroups：看它比對時是否有限制 SpecType、LowRiskListGroup 或 NamePropertyBitCode。
 * 搜尋 GROUPID：看是否有寫死特定 GROUPID（例如只比對 1, 2，漏掉了 5）。
步驟 2：確認自訂名單總表 / View 是否有納入 OFAC_SPEC
請確認資料庫中是否有比對母表（例如 OFAC_SPEC_ALL、OFAC_ALL、GL_SPEC 或 Blacklist_SPEC）：
SELECT type_desc, name 
FROM sys.objects 
WHERE name LIKE '%SPEC%' OR name LIKE '%BLACKLIST%';

 * 如果有類似 OFAC_SPEC_ALL 或 V_OFAC_SPEC 的 View，請用 sp_helptext 'View名稱' 確認是否有包含 GROUPID = 5。
把 DBCHK_ProcessDetectionResults 中關於 SPEC 或 OFAC_SPEC 的那段 SQL 貼出來，就能直接抓出比對引擎少抓 GROUPID = 5 的原因！
