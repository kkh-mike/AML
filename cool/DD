這個改動是為了防止使用者在畫面上不小心點到或反白框選儲存格，導致匯出的 CSV 檔案缺漏或格式壞掉。
原本程式碼中的邏輯是：
If Selection.Cells.Count > 1 Then
    Set SrcRg = Selection
Else
    Set SrcRg = ActiveSheet.UsedRange
End If

 * 發生什麼事：
   如果使用者在執行巨集前，滑鼠在畫面上不小心拉了一個框、按到 Shift 多選了幾個格子，或者上次編輯殘留了選取狀態，Selection.Cells.Count > 1 就會成立。
 * 導致的後果：
   程式會以為「你只想匯出被反白選取的那幾格」，於是只抓那幾格寫進 CSV，原本幾百列幾千列的資料通通沒被匯出，造成使用者信中抱怨的：「工作表二（TransferredFile）明明資料是對的，但匯出來的 CSV 格式卻完全不對或缺東缺西。」
拿掉這個判斷、直接寫死 Set SrcRg = Sheets("TransferredFile").UsedRange，就是強迫程式不管畫面上游標停在哪、框選了什麼，一律完整匯出整張工作表的全部資料。
