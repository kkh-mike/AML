QUEUEID 就是「作業關卡／案件收件夾」的唯一編號。
在銀行或洗錢防制（AML）的審查流程中，案件進來不會全部混在一起，而是會依業務、分行或作業類別被丟到不同的「佇列（Queue）」裡等待處理。
它在系統裡的 3 大核心作用
 * 決定你在系統 UI 能看到哪個待辦清單：
   登入後，系統會查這張表。只有你的帳號有設定的 QUEUEID，你的畫面上才會出現該業務的待辦清單夾（例如：只配了 CIP 的 Queue，你就看不到 SWIFT 的待辦案件）。
 * 決定派案／分案對象：
   當新案件產生時，派案程式只會從「擁有該 QUEUEID 且角色符合」的人選名單中自動輪派或手動指派。
 * 定義權限範圍（隔離業務）：
   避免經辦跨權限審查。例如：你是香港分行（0900）的 CIP 經辦，系統就只會給你 0900 CIP 的 QUEUEID，防止你審核到其他模組或單位的案子。
如何看這些數字對應什麼業務？
執行以下語法，就能把截圖裡的 QUEUEID 翻譯成看得到的群組名稱：
SELECT 
    u.QUEUEID,
    q.QUEUENAME,      -- 這裡就是對應的業務關卡名稱 (如 0900 CIP、SWIFT MX 等)
    q.SRC AS MODULE,  -- 業務模組代號
    u.USERID,
    u.ROLE
FROM dbo.WORKFLOW_MGMT_USERS u
JOIN dbo.WORKFLOW_MGMT_QUEUE q ON u.QUEUEID = q.QUEUEID
WHERE u.USERID = '922798'
ORDER BY u.QUEUEID;

