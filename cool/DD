要修正「邦丸 賢治」只產出「賢治」的問題，請依據你目前執行的檔案類型，修改對應副程式中的 Arr 抓取方式：
1. 如果你轉的是 KOJIN（個人）
原本寫法把第 2 欄和第 3 欄分開當成不同名字：
' 找到 Sub TransferKojin()
' 把原本這段：
Arr(1) = R.Cells(1, 2)
Arr(2) = R.Cells(1, 3)

' 改成相加（合併姓與名）：
Arr(1) = Trim(CStr(R.Cells(1, 2))) & Trim(CStr(R.Cells(1, 3)))
Arr(2) = "" ' 若第 3 欄已經是名，清空避免重複產出一列

2. 如果你轉的是 SEIHO
原本寫法把第 6 欄和第 7 欄分開：
' 找到 Sub TransferSeiho()
' 把原本這段：
Arr(1) = R.Cells(1, 6)
Arr(2) = R.Cells(1, 7)

' 若第 6 是姓、第 7 是名，改為：
Arr(1) = Trim(CStr(R.Cells(1, 6))) & Trim(CStr(R.Cells(1, 7)))
Arr(2) = ""

3. 如果你轉的是 SONGHO（已有串接但欄位語法有錯）
請檢查並修正 TransferSongho 中的語法錯誤：
' 找到 Sub TransferSongho()
' 修正前：
Arr(3) = Trim(CStr(R.Cells(1, 9))) + Trim(CStr(R.Cells(10))) + Trim(CStr(R.Cells(11)))

' 修正後（補上缺少的第一列參數 1）：
Arr(3) = Trim(CStr(R.Cells(1, 9))) & Trim(CStr(R.Cells(1, 10))) & Trim(CStr(R.Cells(1, 11)))

4. 如果來源欄位本來就在同一個儲存格（只是中間隔了 Tab 或空白）
若來源是在同一個格子內（如 R.Cells(1, 2)），可能是不可見字元被截斷，請在賦值時加入清理：
Arr(1) = Replace(Replace(Replace(R.Cells(1, 2).Value, vbTab, ""), " ", ""), "　", "")

若能確認執行的是哪一個檔名（Action 工作表的 D2 欄位值），就能直接鎖定是上述第幾段程式碼。
