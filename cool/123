SELECT 
    o.name AS sp_name,
    o.type_desc,
    o.modify_date
FROM sys.sql_modules m
JOIN sys.objects o ON m.object_id = o.object_id
WHERE m.definition LIKE '%NMCHK_OTHERlistdetectionresults_temp%'
  AND m.definition LIKE '%INSERT%'
ORDER BY o.modify_date DESC;
