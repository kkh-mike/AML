執行 NMCHK_GetResultFilter 查不出任何資料（結果為空），代表案件被這支 SP 內的權限、Queue 或案件狀態條件擋下來了。
請在 SSMS 執行以下語句，直接拆解是哪一個條件在卡：
DECLARE @UserID NVARCHAR(60) = '1061592';
DECLARE @Role INT = 3;

-- 1. 查看系統中最新的前 5 筆案件狀態與所屬 Queue
SELECT TOP 5 
    A.UID, 
    A.Name_Key, 
    A.STATUS, 
    A.Role AS Case_Role, 
    A.HandledBy, 
    A.QUEUEID, 
    A.Created_Date
FROM BSADBJP.dbo.NMCHK_Review_Main A WITH (NOLOCK)
ORDER BY A.Created_Date DESC;

-- 2. 檢查此登入帳號（1061592）到底被分配在哪些 Queue 和 Role
SELECT * 
FROM BSADBJP.dbo.WORKFLOW_MGMT_USERS WITH (NOLOCK)
WHERE USERID = @UserID;

最常導致「空結果」的 3 個卡點對照：
 * Queue 權限對不上（最常見）：
   * 案件上的 A.QUEUEID 必須存在於 WORKFLOW_MGMT_USERS 且該 User 有綁定對應的 ROLE。
   * 如果第 1 個查詢的 QUEUEID 沒有出現在第 2 個查詢的清單裡，SP 就會直接全部濾除。
 * 案件已經被指派經辦人（HandledBy 不為空）：
   * @GRIDTYPE = 'A' 是待分派清單（Main Queue），條件要求 HandledBy 必須是空的。如果案件已經被指派（例如已有特定經辦人），它只會出現在 @GRIDTYPE = 'B'（Personal Queue）。
 * 案件狀態已經結案（STATUS = '3'）：
   * SP 明確排除了結案狀態（WHERE A.STATUS <> '3'）。
把上面這兩段查詢的結果貼出來，就能直接抓出是卡在 Queue 指派還是案件狀態。
