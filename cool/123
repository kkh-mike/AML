以下是兩次執行的詳細時間計算與整理：
第一張圖（日期：2026/08/05）
| 項目 (Command) | 開始時間 (Begin Import) | 結束時間 (Process Done) | 執行耗時 |
|---|---|---|---|
| TW I CIF | 13:34:16 | 14:15:50 | 41 分 34 秒 (00:41:34) |
| TW I CUST_NP | 14:28:53 | 14:44:02 | 15 分 09 秒 (00:15:09) |
| TW I ACCT | 14:55:25 | 14:59:42 | 4 分 17 秒 (00:04:17) |
| TW I RP_NP | 15:28:38 | 15:30:11 | 1 分 33 秒 (00:01:33) |
| TW I TRXN | 17:53:39 | 23:25:00 | 5 小時 31 分 21 秒 (05:31:21) |
| 純執行總耗時 | — | — | 6 小時 34 分 04 秒 |
第二張圖（日期：2026/08/10）
| 項目 (Command) | 開始時間 (Begin Import) | 結束時間 (Process Done) | 執行耗時 |
|---|---|---|---|
| TW I CIF | 08:27:57 | 09:13:16 | 45 分 19 秒 (00:45:19) |
| TW I ACCT | 09:16:42 | 09:20:44 | 4 分 02 秒 (00:04:02) |
| TW I CUST_NP | 09:21:06 | 09:36:45 | 15 分 39 秒 (00:15:39) |
| TW I RP_NP | 09:42:53 | 09:43:47 | 54 秒 (00:00:54) |
| TW I TRXN | 09:49:47 | 18:16:18 | 8 小時 26 分 31 秒 (08:26:31) |
| 純執行總耗時 | — | — | 9 小時 32 分 25 秒 |




WITH TS_LIST AS
(
    SELECT TS_CUST_ID
    FROM BANCS_SK_CUST_INFO a
    JOIN Customer_Migrate_buffer b
        ON a.SK_CUST_ID = b.CustomerNo
    WHERE a.CUST_RE_NO_FLG IN ('2','3')

    EXCEPT

    SELECT TS_CUST_ID
    FROM BANCS_SK_CUST_INFO a
    JOIN Customer_Migrate_buffer b
        ON a.SK_CUST_ID = b.CustomerNo
    WHERE a.CUST_RE_NO_FLG = '1'
)
SELECT
    t.TS_CUST_ID,
    c.CIF_KEY,
    np.HIGHRISK  AS NP_HIGHRISK,
    bsa.HIGHRISK AS BSA_HIGHRISK,
    CASE
        WHEN ISNULL(np.HIGHRISK, '') = ISNULL(bsa.HIGHRISK, '')
            THEN 'MATCH'
        ELSE 'NOT MATCH'
    END AS RESULT
FROM TS_LIST t
JOIN Customer_Migrate_buffer c
    ON t.TS_CUST_ID = c.CustomerNo
LEFT JOIN Cust_NP_Migrate_buffer np
    ON c.CIF_KEY = np.CIF_KEY
LEFT JOIN BSA_SUB_CUST_FACT bsa
    ON t.TS_CUST_ID = bsa.CustomerNo;




你的需求可以拆成幾個步驟：

1. 找出 TS_CUST_ID (CUST_RE_NO_FLG IN ('2','3') 但沒有 1)
2. 用這些 TS_CUST_ID 去找 Customer_Migrate_buffer
3. Join Cust_NP_Migrate_buffer
4. Join BSA_SUB_CUST_FACT
5. 比對兩邊的 HIGHRISK 是否一致

可以直接一次寫完，不需要一個一個查。

WITH TS_LIST AS
(
    SELECT TS_CUST_ID
    FROM BANCS_SK_CUST_INFO a
    JOIN Customer_Migrate_buffer b
        ON a.SK_CUST_ID = b.CustomerNo
    WHERE a.CUST_RE_NO_FLG IN ('2','3')
    EXCEPT
    SELECT TS_CUST_ID
    FROM BANCS_SK_CUST_INFO a
    JOIN Customer_Migrate_buffer b
        ON a.SK_CUST_ID = b.CustomerNo
    WHERE a.CUST_RE_NO_FLG = '1'
)
SELECT
    t.TS_CUST_ID,
    c.CIF_KEY,
    np.HIGHRISK  AS NP_HIGHRISK,
    bsa.HIGHRISK AS BSA_HIGHRISK,
    CASE
        WHEN np.HIGHRISK = bsa.HIGHRISK THEN 'MATCH'
        ELSE 'NOT MATCH'
    END AS RESULT
FROM TS_LIST t
JOIN Customer_Migrate_buffer c
    ON t.TS_CUST_ID = c.CustomerNo
LEFT JOIN Cust_NP_Migrate_buffer np
    ON c.CIF_KEY = np.CIF_KEY
LEFT JOIN BSA_SUB_CUST_FACT bsa
    ON t.TS_CUST_ID = bsa.CustomerNo;

如果你只想看不一致的資料，再加一個 WHERE：

WHERE
    ISNULL(np.HIGHRISK,'') <> ISNULL(bsa.HIGHRISK,'');

這樣就會只列出：

TS_CUST_ID	NP_HIGHRISK	BSA_HIGHRISK	RESULT
Q1645090150	Y	N	NOT MATCH
…	…	…	…

⸻

如果你的目的是驗證 ETL 是否真的使用 Cust_NP_Migrate_buffer.HIGHRISK

其實可以把來源也一起列出，比較好追：

SELECT
    t.TS_CUST_ID,
    c.CIF_KEY,
    np.HIGHRISK  AS Source_HIGHRISK,
    bsa.HIGHRISK AS Target_HIGHRISK,
    CASE
        WHEN ISNULL(np.HIGHRISK,'') = ISNULL(bsa.HIGHRISK,'')
            THEN 'OK'
        ELSE 'DIFFERENT'
    END AS Verify_Result

這樣一眼就能知道 BSA_SUB_CUST_FACT.HIGHRISK 是否與 Cust_NP_Migrate_buffer.HIGHRISK 相同，也是驗證 ETL 最常用的寫法。