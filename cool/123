這段修改主要有兩個核心變更點：
 * GROUPID 異動：目標群組從 5 改為 11。
 * 新增死亡名單來源：從 BSADB.dbo.DJ_Entity 撈取 Deceased = 1 且掛有 Japanese Finance 制裁標籤的資料，透過 NOT EXISTS 補入暫存表 #tempDJBlacklistJP。
以下為測試案例（Test Cases, TC）設計，包含 Before / After 差異驗證、正向與反向情境。
變更點對照 (Before vs. After)
| 評估維度 | 原邏輯 (Before) | 新邏輯 (After) |
|---|---|---|
| 目標 GROUPID | 5 | 11 |
| 資料來源表 | 僅 DJ_List_Data_A (本名條件 NamePropertyBitCode & 1 > 0) | DJ_List_Data_A + DJ_Entity (Deceased = 1) |
| 去重保護 | GROUP BY EntNum, SdnName | 同前，加上 DJ_Entity 不得重複寫入已存在於第一批的 EntNum |
| 日期格式 | 取 MAX(UpdatedDate) 字串 | DJ_Entity 取 CONVERT(VARCHAR, UpdatedDate_UTC, 112) (YYYYMMDD) |
正向測試案例 (Positive TC)
TC-01：死亡名單常態新增（不在 DJ_List_Data_A 的純死亡名單）
 * 測試目的：驗證 DJ_Entity 的死亡清單能成功寫入目標表與 Log 表。
 * 前置資料準備：
   * DJ_Entity：EntityId = 90001, FullName = 'DEAD PERSON A', Deceased = 1, UpdatedDate_UTC = 2026-08-01。
   * DJ_SanctionListReference：EntityId = 90001, SanctionListReference = 'Japanese Finance List'。
   * DJ_List_Data_A：無 90001 資料。
   * BSADBJP..OFAC_SPEC：無 90001 資料。
 * 執行動作：執行 SP [dbo].[GLSPEC_SyncDJToInternalList] @GroupId = 1。
 * 預期結果：
   * BSADBJP..OFAC_SPEC 新增一筆記錄：spec_name = 'DEAD PERSON A', remark = '90001', GROUPID = '11', DInd = 'N', updated_date = '20260801'。
   * BSADBJP.dbo.OFAC_SPEC_LOG 新增一筆日誌，ActionCD = '01'，GROUPID = '11'。
TC-02：舊資料 GROUPID 切換與軟刪除機制
 * 測試目的：驗證既有 GROUPID = 5 不受影響，所有同步均轉入 GROUPID = 11。
 * 前置資料準備：
   * BSADBJP..OFAC_SPEC 保留一筆舊的 GROUPID = 5, remark = '12345', DInd = 'N'。
 * 執行動作：執行 SP。
 * 預期結果：
   * 該筆 GROUPID = 5 的資料狀態維持不變（不會被更新或軟刪除為 DInd = 'Y'）。
   * 新增或異動的資料僅對應 GROUPID = 11。
TC-03：死亡名單重複時以 DJ_List_Data_A 為主（NOT EXISTS 驗證）
 * 測試目的：驗證同一 EntityId 同時符合兩邊條件時，不會產生主鍵衝突或重覆寫入。
 * 前置資料準備：
   * DJ_List_Data_A：EntNum = 90002, SdnName = 'NAME FROM LIST_A', BitCode & 1 > 0。
   * DJ_Entity：EntityId = 90002, FullName = 'NAME FROM ENTITY', Deceased = 1。
   * 兩者皆關聯至 Japanese Finance。
 * 執行動作：執行 SP。
 * 預期結果：
   * #tempDJBlacklistJP 只有 1 筆 EntNum = 90002。
   * spec_name 為 'NAME FROM LIST_A'（確保第一段優先，第二段被 NOT EXISTS 濾除）。
反向測試案例 (Negative TC)
TC-04：Deceased 旗標不符（非死亡名單不應入選）
 * 測試目的：驗證非死亡人口不會誤入第二批同步。
 * 前置資料準備：
   * DJ_Entity：EntityId = 90003, Deceased = 0（或 NULL），且有 Japanese Finance 標籤。
   * DJ_List_Data_A：無此資料。
 * 執行動作：執行 SP。
 * 預期結果：
   * BSADBJP..OFAC_SPEC 不應 新增 remark = '90003' 的資料。
TC-05：制裁清單非 Japanese Finance（標籤比對反向測試）
 * 測試目的：驗證制裁名單過濾條件精準度。
 * 前置資料準備：
   * DJ_Entity：EntityId = 90004, Deceased = 1。
   * DJ_SanctionListReference：SanctionListReference = 'OFAC SDN'（無 Japanese Finance 字樣）。
 * 執行動作：執行 SP。
 * 預期結果：
   * BSADBJP..OFAC_SPEC（GROUPID = 11）不應 出現 remark = '90004'。
TC-06：死亡名單自資料來源下架（MERGE 軟刪除測試）
 * 測試目的：驗證原本同步入庫的死亡人員若不再符合條件，會被更新為已刪除。
 * 前置資料準備：
   * BSADBJP..OFAC_SPEC 已存在 remark = '90005', GROUPID = 11, DInd = 'N'。
   * 將來源端 DJ_Entity 的 Deceased 改為 0，或移除其 SanctionListReference。
 * 執行動作：執行 SP。
 * 預期結果：
   * BSADBJP..OFAC_SPEC 該筆記錄更新為 DInd = 'Y', DelUserID = 'System Admin', DelDate 填入當前時間。
   * BSADBJP.dbo.OFAC_SPEC_LOG 新增一筆 ActionCD = '03'。
TC-07：空值容錯測試（UpdatedDate_UTC 為 NULL）
 * 測試目的：驗證當 DJ_Entity.UpdatedDate_UTC 為 NULL 時不會造成轉換報錯。
 * 前置資料準備：
   * DJ_Entity：EntityId = 90006, Deceased = 1, UpdatedDate_UTC = NULL。
   * 關聯具備 Japanese Finance。
 * 執行動作：執行 SP。
 * 預期結果：
   * 若程式預設允許 NULL 轉換，需確認寫入 #tempDJBlacklistJP 的 UpdatedDate 欄位為 NOT NULL，檢驗是否會引發 Cannot insert NULL into UpdatedDate 錯誤（若報錯，需在 SQL 中補 ISNULL(CONVERT(...), '19000101') 防呆）。
代碼實作風險提示（建議提早優化）
 * @MAXNo 批次遞增 Bug：
   UPDATE #tempDJBlacklistDetailsJP
SET @MAXNo = @MAXNo + 1, Num = @MAXNo
WHERE Num IS NULL;

   在 SQL Server 中，UPDATE 同步累加變數如果同時有多筆 Num IS NULL，所有新記錄都會被賦予同一個 @MAXNo + 1 值，導致主鍵重複或序號相同。建議改用視窗函數更新：
   ;WITH CTE AS (
    SELECT Num, ROW_NUMBER() OVER(ORDER BY EntNum) as rn
    FROM #tempDJBlacklistDetailsJP
    WHERE Num IS NULL
)
UPDATE CTE SET Num = @MAXNo + rn;

 * UpdatedDate_UTC 防呆：
   #tempDJBlacklistJP.UpdatedDate 定義為 NOT NULL，若來源表 UpdatedDate_UTC 有 NULL 值，CONVERT 出來會是 NULL 導致 INSERT 失敗。建議包一層 ISNULL(MAX(CONVERT(VARCHAR(8), A.UpdatedDate_UTC, 112)), CONVERT(VARCHAR(8), GETUTCDATE(), 112))。
