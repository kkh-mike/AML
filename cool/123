這張清單直接抓到關鍵架構了！
注意清單中的這兩張表：
 * OFAC_SPEC_PARSED（還有 ofac_spec_parsed_temp）
 * OFAC_SPEC_FORMAT
為什麼手動改 OFAC_SPEC 沒有用？
在該系統架構中，比對引擎（後端服務或 CLR）在比對 SPEC 名單時，讀取的根本不是原始的 OFAC_SPEC，而是經過解析、清洗、分詞後的 OFAC_SPEC_PARSED！
當在前端介面新增名單或上傳名單時，系統會呼叫類似 OFACTDB_ProcessSPECList 或 OFACTDB_SaveSpecialList，將原始資料清洗後寫入 OFAC_SPEC_PARSED。
如果直接在 OFAC_SPEC 改資料或補國籍，OFAC_SPEC_PARSED 根本不會自動同步，所以比對引擎在讀字典時依然讀不到或讀到舊資料。
第一步：立即驗證 OFAC_SPEC_PARSED
請直接下這段 SQL，看 Stojilkovic 在解析表裡長什麼樣、甚至到底在不在：
SELECT * 
FROM OFAC_SPEC_PARSED 
WHERE spec_name LIKE '%Stojilkovic%' 
   OR spec_name LIKE '%Vlajko%';

 * 如果查無資料：
   代表這筆資料根本沒有完成解析或同步到比對庫！「阪下透」有在裡面，但「Stojilkovic」不在，後端引擎當然永遠抓不到它。
 * 如果有資料：
   看它的欄位內容（例如 spec_name 被清洗成了什麼格式、是否有拆成 First/Last Name、國籍欄位是否仍然是舊的或 NULL）。
第二步：如果是缺少解析資料，如何讓它生效？
查看清單中的預存程序：
 * OFACTDB_ProcessSPECList
 * OFACTDB_UpdateSpecBatch
 * OFACTDB_ApproveSpecialList
若是在後台手動新增但尚未經過主管「審核通過（Approve）」，資料只會停留在 Buffer / 原始表，不會進到 PARSED 比對表。
請檢查該筆名單的審核狀態，或查看 OFACTDB_ProcessSPECList 的內容：
EXEC sp_helptext 'OFACTDB_ProcessSPECList';

看它是如何把 OFAC_SPEC 轉進 OFAC_SPEC_PARSED 的，只要重新執行一次對應的處理程序（或從管理介面上點擊重新產生/審核通過），比對引擎就能載入這筆英文名單。
