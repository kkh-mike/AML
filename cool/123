SELECT 
    name AS table_or_view_name, 
    type_desc, 
    modify_date
FROM sys.objects
WHERE name LIKE '%OFAC%SPEC%' 
   OR name LIKE '%NMCHK%SPEC%'
   OR name LIKE '%Blacklist%SPEC%'
ORDER BY modify_date DESC;
