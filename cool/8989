造成「參數錯誤」且找不到事件的原因有兩個：
 * ProviderName 與寫入時不一致：你寫入時改用了 $sourceName = "Application"，但偵測腳本裡依然在查 ProviderName = 'MyTestApp'。
 * FilterHashtable 的查詢限制：在 Windows 事件日誌中，如果指定了 LogName = 'Application'，底層 Provider 若為傳統來源（如 Application），有時搭配特定鍵值會觸發 Windows API 的「參數錯誤 (The parameter is incorrect)」。
解法 1：使用 Get-WinEvent（修正 ProviderName 與加入 Id）
將 ProviderName 改為 'Application'，並加上 Id = 9999 精確比對：
$filter = @{
    LogName      = 'Application'
    ProviderName = 'Application'
    Id           = 9999
    Level        = 2  # Error
    StartTime    = (Get-Date).AddMinutes(-5)
}

$events = Get-WinEvent -FilterHashtable $filter -ErrorAction SilentlyContinue

if ($events) {
    foreach ($evt in $events) {
        Write-Host "[偵測到錯誤] ID: $($evt.Id) | 時間: $($evt.TimeCreated)" -ForegroundColor Red
        Write-Host "內容: $($evt.Message)`n"
    }
} else {
    Write-Host "過去 5 分鐘內無符合的錯誤事件。" -ForegroundColor Green
}

驗證方式：執行後不再出現紅字「參數錯誤」，且會列出剛才寫入的 9999 錯誤內容。
解法 2：改用傳統的 Get-EventLog（最相容、不報參數錯誤）
Write-EventLog 寫入的是傳統 Windows Event Log 機制，搭配舊版指令 Get-EventLog 容錯率最高，完全不會有 API 參數解析錯誤：
$events = Get-EventLog -LogName Application `
                       -Source "Application" `
                       -EntryType Error `
                       -InstanceId 9999 `
                       -After (Get-Date).AddMinutes(-5) `
                       -ErrorAction SilentlyContinue

if ($events) {
    foreach ($evt in $events) {
        Write-Host "[偵測到錯誤] ID: $($evt.InstanceId) | 時間: $($evt.TimeGenerated)" -ForegroundColor Red
        Write-Host "內容: $($evt.Message)`n"
    }
} else {
    Write-Host "過去 5 分鐘內無符合的錯誤事件。" -ForegroundColor Green
}

驗證方式：執行後終端直接印出紅色的 [偵測到錯誤] ID: 9999 與自訂的錯誤內容。
