從提供的真實 Event Log 內容可以萃取出幾個關鍵特徵：
 * LogName：Application
 * ProviderName (Source)：PATRIOT OFFICER SWIFT Database Service
 * Id (EventCode)：0
 * Level：2（Error）
 * 關鍵字特徵：訊息中包含 SqlException、逾時、amldb1 或 does not completed in SWIFT detection。
1. 模擬腳本（simulate.ps1）
為了模擬出完全一致的來源，需要先註冊 PATRIOT OFFICER SWIFT Database Service（需以系統管理員權限執行一次）。
$logName = "Application"
$sourceName = "PATRIOT OFFICER SWIFT Database Service"
$eventId = 0

# 1. 檢查並註冊該 Source（需系統管理員身分執行一次）
if (-not [System.Diagnostics.EventLog]::SourceExists($sourceName)) {
    New-EventLog -LogName $logName -Source $sourceName
    Write-Host "已建立事件來源: $sourceName" -ForegroundColor Cyan
}

# 2. 模擬真實的錯誤訊息內容
$mockMessage = @"
Exception Type : SqlException
Source : .Net SqlClient Data Provider
Date : $(Get-Date -Format "yyyy年MM月dd日")
Time : $(Get-Date -Format "tt hh:mm:ss")
Message : 執行逾時到期。在作業完成之前超過逾時等待的時間，或是伺服器未回應。

Custom message :
Message Id :$(New-Guid) does not completed in SWIFT detection

SQL Exception Detail :
Error Code : -2146232060
Server : amldb1
Source : .Net SqlClient Data Provider
"@

# 3. 寫入 Event Log
Write-EventLog -LogName $logName `
               -Source $sourceName `
               -EntryType Error `
               -EventId $eventId `
               -Message $mockMessage

Write-Host "已成功模擬寫入 PATRIOT OFFICER 逾時錯誤！" -ForegroundColor Green

> 注意：如果測試機無法取得系統管理員權限註冊該 Source，可將 $sourceName 暫時改成 "Application"，只在訊息內放入相同的關鍵字進行過濾邏輯驗證。
> 
2. 監控 / 測試腳本（monitor.ps1）
這類 .NET 傳統服務寫入的事件，Id 通常固定為 0。監控時除了比對 ProviderName 與 Level，建議加上關鍵字過濾（如 SqlException 或 逾時），避免誤判其他非資料庫類型的錯誤。
# 設定監控區間（例如抓過去 10 分鐘內）
$minutesBack = 10
$startTime = (Get-Date).AddMinutes(-$minutesBack)

$filter = @{
    LogName      = 'Application'
    ProviderName = 'PATRIOT OFFICER SWIFT Database Service'
    Id           = 0
    Level        = 2   # 2 = Error
    StartTime    = $startTime
}

# 抓取事件
$events = Get-WinEvent -FilterHashtable $filter -ErrorAction SilentlyContinue

# 過濾符合 SQL 逾時特徵的事件
$targetErrors = $events | Where-Object { 
    $_.Message -like "*SqlException*" -and $_.Message -like "*逾時*" 
}

if ($targetErrors) {
    Write-Host "========================================" -ForegroundColor Red
    Write-Host " [告警] 偵測到 PATRIOT OFFICER SWIFT 資料庫逾時錯誤！" -ForegroundColor Red
    Write-Host " 筆數: $($targetErrors.Count)" -ForegroundColor Red
    Write-Host "========================================" -ForegroundColor Red

    foreach ($evt in $targetErrors) {
        Write-Host "時間: $($evt.TimeCreated)" -ForegroundColor Yellow
        Write-Host "主機: $($evt.MachineName)"
        
        # 擷取關鍵訊息（印出前 6 行避免洗畫面）
        $lines = $evt.Message -split "`r`n|`n"
        Write-Host "摘要: $($lines[0..4] -join ' | ')"
        Write-Host "----------------------------------------"
    }

    # 此處可加入後續處理：發送 Teams Webhook、Email 或呼叫監控 API
} else {
    Write-Host "過去 $minutesBack 分鐘內無符合的逾時錯誤事件。" -ForegroundColor Green
}

測試驗證步驟
 * 打開系統管理員權限的 PowerShell 執行 simulate.ps1。
 * 執行 monitor.ps1，確認終端能精確抓到該筆資料並顯示紅色告警。
 * 監控端若要常駐（例如透過 Windows 工作排程器 Task Scheduler），建議設定每 5 或 10 分鐘執行一次，並將 $minutesBack 設為相同週期即可。
