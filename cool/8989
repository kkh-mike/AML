這張截圖非常關鍵，真相已經完全浮現了：
結果分析
 * OFAC_SPEC 裡面確實有 72,086 筆 GROUPID = 5 的名單（第三個查詢：資料有正確寫入目標表）。
 * lowrisklistgroups 也有設定 GROUPID = 5（第一個查詢：設定存在）。
 * 但是 NMCHK_ResultHistory 查出來是 0 筆（第二個查詢：完全沒有任何歷史案件命中過 sdn_grp_num = 5）。
這代表：名單同步 SP 沒有問題，問題出在「執行姓名檢核的比對引擎 / 批次 SP」根本沒有將 GROUPID = 5 納入掃描範圍，或者比對來源表沒有更新到！
請依序執行以下 2 個排查動作：
動作 1：確認比對引擎所使用的底層黑名單來源表
通常系統在做比對時，不是直接掃 OFAC_SPEC，而是會先經過一段 ETL 轉入比對用的黑名單總表（例如 OFAC_SDN、NMCHK_BlackList 或 OFAC_ALL）。
請執行以下查詢，確認是否有資料：
-- 檢查 OFAC_SDN 或類似的比對母表是否有包含 GROUPID = 5 的資料
SELECT TOP 10 * 
FROM BSADBJP..OFAC_SDN 
WHERE sdn_type = 'SPEC' AND sdn_grp_num = 5;

(如果找不到 OFAC_SDN，請找找看資料庫裡是否有 OFAC_ALT、NM_BLACKLIST 這類名字的表)
動作 2：找出「產出 NMCHK_ResultHistory」的那支比對 Stored Procedure
請搜尋資料庫中寫入 NMCHK_ResultHistory 的 SP 名稱：
SELECT OBJECT_NAME(id) AS SP_Name
FROM syscomments
WHERE text LIKE '%INSERT INTO%NMCHK_ResultHistory%'
GROUP BY OBJECT_NAME(id);

把這兩個查詢的結果貼上來，我們就能直接看是哪支 SP 少抓了 GROUPID = 5，或是哪個比對前置作業（ETL）沒跑！
