從搜尋結果來看，這 3 支 SP 都只是查詢用的（不是維護 OFAC_SDN 母表的程式）。這表示比對用的黑名單資料庫可能是透過 VIEW（檢視表） 串接，或是比對引擎直接查詢了不同的來源。
請依序執行以下 3 個步驟來鎖定比對來源：
步驟 1：確認 OFAC_SDN 到底是一張實體表還是 View
在 SSMS 執行以下語句：
SELECT type_desc, name 
FROM sys.objects 
WHERE name IN ('OFAC_SDN', 'OFAC_SPEC', 'OFAC_ALT');

 * 如果 OFAC_SDN 的 type_desc 是 VIEW，請接著執行：
   sp_helptext 'OFAC_SDN';

   查看 View 定義裡是不是只 UNION 了其他 GROUPID，而漏掉了 GROUPID = 5。
步驟 2：直接查看比對引擎核心 SP NMCHK_GetDetectionResult
剛才搜尋到的第 3 個 SP NMCHK_GetDetectionResult 就是產出比對結果的關鍵。請直接查看它的內容：
sp_helptext 'NMCHK_GetDetectionResult';

 * 在內容中搜尋 FROM、WHERE、GROUPID 或 OFAC_SPEC，看看它比對黑名單時到底關聯了哪幾張表與哪些過濾條件。
步驟 3：確認比對引擎是否有對應的黑名單設定檔
有些系統會有名單群組啟用開關或權重設定表，確認是否有開啟 Japan 名單的比對：
SELECT * FROM BSADBJP.dbo.lowrisklistgroups;

(確認第 1 步查到的 GROUPID = 5 那筆，裡面的 LowRiskListGroup 或相關開關欄位數值是否與其他正常命中的群組一致)
