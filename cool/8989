SELECT TOP 10 
    spec_name, 
    ent_num, 
    remark AS EntityId, 
    updated_date
FROM BSADBJP..OFAC_SPEC
WHERE GROUPID = 5 
  AND DInd = 'N'
  AND spec_name NOT IN (SELECT sdn_name FROM BSADBJP..OFAC_SDN);
