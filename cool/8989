剛才搜尋時，因為指令欄位被截斷，我們只看到路徑清單，但最關鍵的 Program.cs 裡面是怎麼寫 SmtpClient 的 剛好藏在內容中！
直接執行以下這行指令，將 Program.cs 裡面 發信連線的那幾行程式碼 直接印出來：
Get-Content "D:\project\AML\src\NET_EMAIL\CIP_RM_TIMEOUT_REPORT\Program.cs" | 
    Select-String -Pattern "SmtpClient", "Send", "Credentials", "Host", "Port", "MailAddress" -Context 3, 3

為什麼這段指令能直接解答？
在 C# 寫發信通常只有三種設定方式，印出來後一眼就能看穿：
 * 指向另外一台內網發信機（不是 relay.taishinbank.com.tw）：
   如果看到類似 new SmtpClient("10.13.x.x") 或讀取自某個 IP，那台就是專門免認證的發信主機。
 * 帶入了專用的服務帳號密碼：
   如果看到 smtp.Credentials = new NetworkCredential(...)，就代表必須帶特定帳號驗證。
 * 從資料庫撈取設定：
   如果看到它是從剛才的 amlist / RPADB 撈出 SMTP 設定，我們直接進 DB 查那筆設定即可。
請在 PowerShell 貼上執行上方指令，看印出的程式碼上下文是怎麼寫的。
