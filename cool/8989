從這張截圖可以確認比對失敗的根因：
OFAC_SDN 裡面完全沒有 sdn_type = 'SPEC' 的資料（結果為 0 筆）。
原因解析
 * 比對架構運作方式：
   * 系統的姓名檢核引擎（Matching Engine）實際掃描的底層黑名單來源是 OFAC_SDN（以及相關的 OFAC_ALT 等比對母表）。
   * 你前面調整的 SP 雖然已經將資料成功寫入了 OFAC_SPEC（有 72,086 筆），但是 OFAC_SPEC 的資料還沒有被同步/轉入 OFAC_SDN。
   * 比對母表內沒有這些資料，檢核引擎自然永遠比對不到 GROUPID = 5。
解決與排查步驟
步驟 1：找出將 OFAC_SPEC 轉入 OFAC_SDN 的同步 SP
通常系統會有一支 SP 或 Batch Job 負責把各群組的 SPEC 資料彙整進 OFAC_SDN。請執行以下語句找出這支 SP：
SELECT OBJECT_NAME(id) AS SP_Name
FROM syscomments
WHERE text LIKE '%OFAC_SPEC%'
  AND text LIKE '%INSERT INTO%OFAC_SDN%'
GROUP BY OBJECT_NAME(id);

如果查無結果，可以擴大搜尋：
SELECT OBJECT_NAME(id) AS SP_Name
FROM syscomments
WHERE text LIKE '%OFAC_SPEC%'
  AND text LIKE '%OFAC_SDN%'
GROUP BY OBJECT_NAME(id);

步驟 2：確認日常批次排程（Control-M / SQL Job）
確認在每日名單更新流程中：
 * 跑完寫入 OFAC_SPEC 的 SP 之後。
 * 是否有漏跑「重新載入/建置 OFAC_SDN 黑名單母表」的批次作業。
只要執行該段彙整同步程式，讓 OFAC_SPEC（GROUPID = 5）的 72,086 筆資料轉入 OFAC_SDN 後，再次執行姓名檢核比對，日本名單就會正常命中。
