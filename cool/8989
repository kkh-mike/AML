要確認檢核結果（NMCHK_ResultHistory）命中的名單是否來自 GROUPID = 5，關鍵在於比對 ent_num 或 sdn_name，以及比對 sdn_type。
方法 1：直接透過 ent_num 與 sdn_name 關聯回 OFAC_SPEC（最精準）
檢核表中的 ent_num 對應的就是 OFAC_SPEC 的 ent_num：
SELECT 
    R.uid,
    R.new_acct_key,
    R.sdn_name,
    R.sdn_type,
    R.ent_num,
    O.GROUPID,
    O.SOURCE,
    O.remark AS DJ_EntityId
FROM NMCHK_ResultHistory R
JOIN BSADBJP..OFAC_SPEC O 
  ON R.ent_num = O.ent_num 
 AND R.sdn_name = O.spec_name
WHERE R.uid = '202694'
  AND O.GROUPID = 5;

 * 如果有查出資料：表示命中的紀錄就是從 GROUPID = 5（日本金融制裁）來的。
 * 如果為 0 筆：表示該比對命中結果來自其他名單群組。
方法 2：觀察 sdn_type 欄位
從你的截圖中可以看到現有比對結果的 sdn_type 分別是：
 * UN / UN ALT（聯合國制裁）
 * EU / EU ALT（歐盟制裁）
 * UKHMT / UKHMT ALT（英國財政部制裁）
 * OFAC / OFAC ALT（美國財政部制裁）
在你們寫入 OFAC_SPEC 的 SP 邏輯中，SOURCE 欄位被填入 'JP FINANCE'：
INSERT INTO ... (..., GROUPID, SOURCE, ...)
VALUES (..., '5', 'JP FINANCE', ...)

因此，如果檢核引擎產生的 sdn_type 是對應來源名單，只要看到 sdn_type 帶有 JP 或 JP FINANCE，即代表來自 GROUPID = 5。截圖中的這 22 筆目前全部都屬於 UN、EU、UKHMT 與 OFAC，沒有包含日本名單（GROUPID = 5）。
