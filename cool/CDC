從截圖抓到了兩個關鍵原因，這就是為什麼你的帳號查不到、以及直接改可能會失效的原因：
 * 分行代號（BANKNO）不符：
   * 當前資料庫是 HK（香港，REPLACE('BSADBHK', 'BSADB', '') = 'HK'）。
   * 下方你的帳號 1141923 目前登記的 BANKNO 是 TW（台灣）。SP 第一關過濾 A.BANKNO = 'HK' 就直接把你的帳號濾掉了。
 * 工作群組與角色欄位是空的：
   * 上下兩張圖的 WORKGROUP 與 ROLE 全都是 NULL。這表示 GCMAINDB.dbo.AutomatedEmployees 很可能本身也是被某個人資 AD 排程整批洗寫進來的來源表（若直接在上面 UPDATE 也極可能被上游整批蓋回 NULL）。
最穩健的解法：在來源表新增一筆「香港 (HK)」專屬記錄
因為你的帳號本身在 TW，要在當前的 HK 環境使用且不被上游台灣的人事檔覆蓋，直接為你的帳號在 HK 新增一筆記錄：
INSERT INTO GCMAINDB.dbo.AutomatedEmployees (
    CN,
    BANKNO,
    BRANCH,
    USERGROUP,
    WORKGROUP,
    ROLE,
    FULLNAME,
    MODULE,
    EMAIL
)
VALUES (
    '1141923',
    'HK',                                   -- 符合當前資料庫的 BANKNO
    '0900',                                 -- 參考上方 HK 的分行代號
    '海外事業處香港分行作業組作業三組',        -- 參考上方同組名稱
    '0900 CIP',                             -- 目標工作群組名稱 (請確保與 WORKFLOW_MGMT_QUEUE 的 QUEUENAME 完全一致)
    2,                                      -- 角色代碼：2 為 Checker (主管)
    '高OO',                                 -- 你的姓名
    NULL,
    '1141923@tbtmail.com'                   -- 你的 Email
);

> 注意： 若 GCMAINDB.dbo.AutomatedEmployees 有 (CN, BANKNO) 或 (CN, WORKGROUP) 的唯一限制（Unique Constraint），直接新增如果報錯，再改用以下語法修改現有那筆資料測試：
> UPDATE GCMAINDB.dbo.AutomatedEmployees
> SET BANKNO = 'HK',
>     BRANCH = '0900',
>     WORKGROUP = '0900 CIP',
>     ROLE = 2
> WHERE CN = '1141923';
> 
> 
步驟驗證
執行寫入或更新後，執行以下語法進行同步與驗證：
-- 1. 執行同步預存程序
EXEC dbo.WORKFLOW_AutomatedUSERS;

-- 2. 驗證本機表是否成功帶入
SELECT 
    q.QUEUENAME AS WorkGroup,
    u.USERID,
    u.ROLE AS ROLE_ID,
    r.ROLE_DESC,
    r.ROLE_DESC_zh_TW
FROM dbo.WORKFLOW_MGMT_USERS u
JOIN dbo.WORKFLOW_MGMT_QUEUE q ON u.QUEUEID = q.QUEUEID
JOIN dbo.WORKFLOW_MGMT_ROLE r ON u.ROLE = r.ID
WHERE u.USERID = '1141923';

