USE [BSADBTW]
GO

-- 1. 先確認要被刪除的筆數是否剛好是 400 筆
SELECT COUNT(*) AS [預計刪除筆數]
FROM [dbo].[WORKFLOW_MGMT_QUEUE]
WHERE QUEUENAME IN (SELECT QUEUENAME FROM [dbo].[test]);

-- 2. 確定筆數正確後，執行刪除
DELETE FROM [dbo].[WORKFLOW_MGMT_QUEUE]
WHERE QUEUENAME IN (SELECT QUEUENAME FROM [dbo].[test]);

-- 3. 最後把暫存表也刪除
DROP TABLE [dbo].[test];
