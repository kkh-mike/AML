根據你提供的 SQL 檢核邏輯與需求，這組 Test Case (TC) 核心在於驗證：「篩選條件符合性」、「目標表寫入/欄位對應正確性」、「增刪改 (INSERT / UPDATE / 重新上架) 狀態轉換」以及「反向排除驗證」。
延續你 TC-JP-01 的規格風格，以下為你規劃的完整測試案例設計：
TC-SAN-01：制裁實體 19 類證件/通訊碼同步 - 正向新增 (INSERT)
測試目的
驗證符合 OFAC / EU 制裁清單條件且 PersonStatus <> 'Inactive' 的有效實體，其所屬之 19 類證件/通訊資料（Deleted = 0），當目標表（OFAC_GLSpec）不存在時，能成功新增一筆 GROUPID = 10 的資料，且欄位格式與日期正確。
Before
 * 來源端資料確認：
   * DJ_Entity.PersonStatus \neq 'Inactive'
   * DJ_ListCategory.Description2 = 'Sanctions Lists' 或 Description3 符合 OFAC/EU 8 大關聯分類且 Deleted = 0
   * DJ_IdentificationTypeInfo.IdentificationType 屬於指定 19 類之一且 Deleted = 0
 * 目標端資料確認：
   * OFAC_GLSpec 中查無此資料（GROUPID = 10、spec_name = D.IdentificationNumber、remark = A.EntityId、ADDRESS 包含該證件類型）。
After
 * OFAC_GLSpec 成功新增資料並滿足：
   * GROUPID = 10
   * spec_name = DJ_IdentificationTypeInfo.IdentificationNumber
   * remark = CAST(DJ_Entity.EntityId AS VARCHAR(100))
   * ADDRESS = DJ_IdentificationTypeInfo.IdentificationType + '; Please go to Dowjones website for more detail'
   * updated_date = 該實體在 DJ_List_Data_A 之 MAX(UpdatedDate)
   * DInd = 'N'（或預設未下架狀態）
TC-SAN-02：已下架資料重新上架驗證 (UPDATE - DInd = 'Y' 重新上架)
測試目的
驗證目標表已存在且曾標記為刪除/下架（DInd = 'Y'）之識別碼資料，當來源端重新符合同步條件時，能自動將狀態更新還原為上架狀態（DInd = 'N'），並同步最新異動日期。
Before
 * 來源端資料符合 TC-SAN-01 條件。
 * 目標表 OFAC_GLSpec 存在相同識別碼資料，但 DInd = 'Y'。
After
 * OFAC_GLSpec.DInd 更新為 'N'。
 * OFAC_GLSpec.updated_date 更新為來源端最新 MAX(C.UpdatedDate)。
TC-SAN-03：來源異動日期更新驗證 (UPDATE - Date Update)
測試目的
驗證目標表已存在相同識別碼資料且處於有效狀態（DInd <> 'Y'），當來源端 DJ_List_Data_A 之更新日期變更（MAX(C.UpdatedDate) <> T.updated_date）時，系統能正確更新目標表之 updated_date，其餘關鍵欄位維持不變。
Before
 * 來源端符合同步條件，但 DJ_List_Data_A.UpdatedDate 有最新更新時間 T_2。
 * 目標表 OFAC_GLSpec 存在該筆資料，DInd <> 'Y'，但其 updated_date 為舊時間 T_1（T_1 < T_2）。
After
 * OFAC_GLSpec.updated_date 正確被更新為 T_2。
 * 其餘欄位（spec_name, remark, ADDRESS）保持一致。
TC-SAN-04：制裁識別碼同步 - 反向排除驗證 (Negative Test)
測試目的
驗證只要未完全滿足篩選條件的資料，系統皆不會將其同步或新增至 OFAC_GLSpec（GROUPID = 10）。
測試情境與檢核點
| 案例子項 | 測試情境 (未達標條件) | 預期結果 (After) |
|---|---|---|
| TC-SAN-04-A | PersonStatus = 'Inactive'（非活躍人員） | 目標表不寫入 / 不更新 |
| TC-SAN-04-B | 非 OFAC/EU 制裁分類（例如：僅屬 PEP 政治人物或非清單類別） | 目標表不寫入 |
| TC-SAN-04-C | DJ_ListCategory.Deleted = 1（分類已刪除） | 目標表不寫入 |
| TC-SAN-04-D | 證件類型不在 19 類清單內（例如：Local Tax ID、Driver Number 等非指定類型） | 目標表不寫入 |
| TC-SAN-04-E | DJ_IdentificationTypeInfo.Deleted = 1（證件已被標記刪除） | 目標表不寫入 |
