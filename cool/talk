
/*
NameProperty(RP)
*/


----------- PEP 企業 主要股東
----------MS 主要股東

SELECT 
    a.CustomerNo,
    c.RefNo,
    c.PEPBitwiseCode,
    CASE WHEN c.PEPBitwiseCode & 1   = 1   THEN 'Y' ELSE 'N' END AS BlackList,
    CASE WHEN c.PEPBitwiseCode & 2   = 2   THEN 'Y' ELSE 'N' END AS PEP,
    CASE WHEN c.PEPBitwiseCode & 4   = 4   THEN 'Y' ELSE 'N' END AS NN,
    CASE WHEN c.PEPBitwiseCode & 8   = 8   THEN 'Y' ELSE 'N' END AS ForeignPEP,
    CASE WHEN c.PEPBitwiseCode & 16  = 16  THEN 'Y' ELSE 'N' END AS LocalPEP,
    CASE WHEN c.PEPBitwiseCode & 32  = 32  THEN 'Y' ELSE 'N' END AS ForeignOrgPEP,
    CASE WHEN c.PEPBitwiseCode & 64  = 64  THEN 'Y' ELSE 'N' END AS EfPEP,
    CASE WHEN c.PEPBitwiseCode & 128 = 128 THEN 'Y' ELSE 'N' END AS CustRPIsPEP
FROM Customer_Migrate_buffer a
JOIN RP_NP_Migrate_buffer c    ON c.CIFKey= a.CIF_KEY
join CIF_Migrate_Mapping_TEMP v on v.SOURCE_CIF_KEY = a.CIF_KEY
join BSA_SUB_CUST_FACT bb on bb.CIF_KEY = v.NEW_CIF_KEY
join PB_CUSTOMER_FACT cc on cc.CIF_KEY = bb.CIF_KEY
WHERE  c.PEPBitwiseCode = '2'  AND cc.CUST_TYPE_CDE = 'N' AND v.ACTION = 'insert' AND c.RP = 'MS'


----------- PEP 企業 其他關連方
-------------------------OTH 其他關連方
SELECT 
    a.CustomerNo,
    c.RefNo,
    c.PEPBitwiseCode,
    CASE WHEN c.PEPBitwiseCode & 1   = 1   THEN 'Y' ELSE 'N' END AS BlackList,
    CASE WHEN c.PEPBitwiseCode & 2   = 2   THEN 'Y' ELSE 'N' END AS PEP,
    CASE WHEN c.PEPBitwiseCode & 4   = 4   THEN 'Y' ELSE 'N' END AS NN,
    CASE WHEN c.PEPBitwiseCode & 8   = 8   THEN 'Y' ELSE 'N' END AS ForeignPEP,
    CASE WHEN c.PEPBitwiseCode & 16  = 16  THEN 'Y' ELSE 'N' END AS LocalPEP,
    CASE WHEN c.PEPBitwiseCode & 32  = 32  THEN 'Y' ELSE 'N' END AS ForeignOrgPEP,
    CASE WHEN c.PEPBitwiseCode & 64  = 64  THEN 'Y' ELSE 'N' END AS EfPEP,
    CASE WHEN c.PEPBitwiseCode & 128 = 128 THEN 'Y' ELSE 'N' END AS CustRPIsPEP
FROM Customer_Migrate_buffer a
JOIN RP_NP_Migrate_buffer c    ON c.CIFKey= a.CIF_KEY
join CIF_Migrate_Mapping_TEMP v on v.SOURCE_CIF_KEY = a.CIF_KEY
join BSA_SUB_CUST_FACT bb on bb.CIF_KEY = v.NEW_CIF_KEY
join PB_CUSTOMER_FACT cc on cc.CIF_KEY = bb.CIF_KEY
WHERE  c.PEPBitwiseCode = '2'  AND cc.CUST_TYPE_CDE = 'N' AND v.ACTION = 'insert' AND c.RP = 'OTH'



---PEP 企業 法定代理人
-------------------------------------------------------- LA 法定代理人



SELECT 
    a.CustomerNo,
    c.RefNo,
    c.PEPBitwiseCode,
    CASE WHEN c.PEPBitwiseCode & 1   = 1   THEN 'Y' ELSE 'N' END AS BlackList,
    CASE WHEN c.PEPBitwiseCode & 2   = 2   THEN 'Y' ELSE 'N' END AS PEP,
    CASE WHEN c.PEPBitwiseCode & 4   = 4   THEN 'Y' ELSE 'N' END AS NN,
    CASE WHEN c.PEPBitwiseCode & 8   = 8   THEN 'Y' ELSE 'N' END AS ForeignPEP,
    CASE WHEN c.PEPBitwiseCode & 16  = 16  THEN 'Y' ELSE 'N' END AS LocalPEP,
    CASE WHEN c.PEPBitwiseCode & 32  = 32  THEN 'Y' ELSE 'N' END AS ForeignOrgPEP,
    CASE WHEN c.PEPBitwiseCode & 64  = 64  THEN 'Y' ELSE 'N' END AS EfPEP,
    CASE WHEN c.PEPBitwiseCode & 128 = 128 THEN 'Y' ELSE 'N' END AS CustRPIsPEP
FROM Customer_Migrate_buffer a
JOIN RP_NP_Migrate_buffer c    ON c.CIFKey= a.CIF_KEY
join CIF_Migrate_Mapping_TEMP v on v.SOURCE_CIF_KEY = a.CIF_KEY
join BSA_SUB_CUST_FACT bb on bb.CIF_KEY = v.NEW_CIF_KEY
join PB_CUSTOMER_FACT cc on cc.CIF_KEY = bb.CIF_KEY
WHERE  c.PEPBitwiseCode = '2'  AND cc.CUST_TYPE_CDE = 'P' AND v.ACTION = 'insert' AND c.RP = 'LA'



--PEP 企業 被授權人或委託人
-------------------------------------------------------- APP 被授權人或委託人

SELECT 
    a.CustomerNo,
    c.RefNo,
    c.PEPBitwiseCode,
    CASE WHEN c.PEPBitwiseCode & 1   = 1   THEN 'Y' ELSE 'N' END AS BlackList,
    CASE WHEN c.PEPBitwiseCode & 2   = 2   THEN 'Y' ELSE 'N' END AS PEP,
    CASE WHEN c.PEPBitwiseCode & 4   = 4   THEN 'Y' ELSE 'N' END AS NN,
    CASE WHEN c.PEPBitwiseCode & 8   = 8   THEN 'Y' ELSE 'N' END AS ForeignPEP,
    CASE WHEN c.PEPBitwiseCode & 16  = 16  THEN 'Y' ELSE 'N' END AS LocalPEP,
    CASE WHEN c.PEPBitwiseCode & 32  = 32  THEN 'Y' ELSE 'N' END AS ForeignOrgPEP,
    CASE WHEN c.PEPBitwiseCode & 64  = 64  THEN 'Y' ELSE 'N' END AS EfPEP,
    CASE WHEN c.PEPBitwiseCode & 128 = 128 THEN 'Y' ELSE 'N' END AS CustRPIsPEP
FROM Customer_Migrate_buffer a
JOIN RP_NP_Migrate_buffer c    ON c.CIFKey= a.CIF_KEY
join CIF_Migrate_Mapping_TEMP v on v.SOURCE_CIF_KEY = a.CIF_KEY
join BSA_SUB_CUST_FACT bb on bb.CIF_KEY = v.NEW_CIF_KEY
join PB_CUSTOMER_FACT cc on cc.CIF_KEY = bb.CIF_KEY
WHERE  c.PEPBitwiseCode = '2'  AND cc.CUST_TYPE_CDE = 'P' AND v.ACTION = 'insert' AND c.RP = 'APP'




-- PEP 企業 董事會成員
-------------------------BOD 董事會成員
SELECT 
    a.CustomerNo,
    c.RefNo,
    c.PEPBitwiseCode,
    CASE WHEN c.PEPBitwiseCode & 1   = 1   THEN 'Y' ELSE 'N' END AS BlackList,
    CASE WHEN c.PEPBitwiseCode & 2   = 2   THEN 'Y' ELSE 'N' END AS PEP,
    CASE WHEN c.PEPBitwiseCode & 4   = 4   THEN 'Y' ELSE 'N' END AS NN,
    CASE WHEN c.PEPBitwiseCode & 8   = 8   THEN 'Y' ELSE 'N' END AS ForeignPEP,
    CASE WHEN c.PEPBitwiseCode & 16  = 16  THEN 'Y' ELSE 'N' END AS LocalPEP,
    CASE WHEN c.PEPBitwiseCode & 32  = 32  THEN 'Y' ELSE 'N' END AS ForeignOrgPEP,
    CASE WHEN c.PEPBitwiseCode & 64  = 64  THEN 'Y' ELSE 'N' END AS EfPEP,
    CASE WHEN c.PEPBitwiseCode & 128 = 128 THEN 'Y' ELSE 'N' END AS CustRPIsPEP
FROM Customer_Migrate_buffer a
JOIN RP_NP_Migrate_buffer c    ON c.CIFKey= a.CIF_KEY
join CIF_Migrate_Mapping_TEMP v on v.SOURCE_CIF_KEY = a.CIF_KEY
join BSA_SUB_CUST_FACT bb on bb.CIF_KEY = v.NEW_CIF_KEY
join PB_CUSTOMER_FACT cc on cc.CIF_KEY = bb.CIF_KEY
WHERE  c.PEPBitwiseCode = '2'  AND cc.CUST_TYPE_CDE = 'N' AND v.ACTION = 'insert' AND c.RP = 'BOD'



--負面新聞 企業 主要股東
--負面新聞 企業 其他關連方
--負面新聞 企業 法定代理人
--負面新聞 企業 董事會成員



----------- 負面新聞 企業 主要股東
----------MS 主要股東

SELECT 
    a.CustomerNo,
    c.RefNo,
    c.PEPBitwiseCode,
    CASE WHEN c.PEPBitwiseCode & 1   = 1   THEN 'Y' ELSE 'N' END AS BlackList,
    CASE WHEN c.PEPBitwiseCode & 2   = 2   THEN 'Y' ELSE 'N' END AS PEP,
    CASE WHEN c.PEPBitwiseCode & 4   = 4   THEN 'Y' ELSE 'N' END AS NN,
    CASE WHEN c.PEPBitwiseCode & 8   = 8   THEN 'Y' ELSE 'N' END AS ForeignPEP,
    CASE WHEN c.PEPBitwiseCode & 16  = 16  THEN 'Y' ELSE 'N' END AS LocalPEP,
    CASE WHEN c.PEPBitwiseCode & 32  = 32  THEN 'Y' ELSE 'N' END AS ForeignOrgPEP,
    CASE WHEN c.PEPBitwiseCode & 64  = 64  THEN 'Y' ELSE 'N' END AS EfPEP,
    CASE WHEN c.PEPBitwiseCode & 128 = 128 THEN 'Y' ELSE 'N' END AS CustRPIsPEP
FROM Customer_Migrate_buffer a
JOIN RP_NP_Migrate_buffer c    ON c.CIFKey= a.CIF_KEY
join CIF_Migrate_Mapping_TEMP v on v.SOURCE_CIF_KEY = a.CIF_KEY
join BSA_SUB_CUST_FACT bb on bb.CIF_KEY = v.NEW_CIF_KEY
join PB_CUSTOMER_FACT cc on cc.CIF_KEY = bb.CIF_KEY
WHERE  c.PEPBitwiseCode = '4'  AND cc.CUST_TYPE_CDE = 'N' AND v.ACTION = 'insert' AND c.RP = 'MS'


----------- 負面新聞 企業 其他關連方
-------------------------OTH 其他關連方
SELECT 
    a.CustomerNo,
    c.RefNo,
    c.PEPBitwiseCode,
    CASE WHEN c.PEPBitwiseCode & 1   = 1   THEN 'Y' ELSE 'N' END AS BlackList,
    CASE WHEN c.PEPBitwiseCode & 2   = 2   THEN 'Y' ELSE 'N' END AS PEP,
    CASE WHEN c.PEPBitwiseCode & 4   = 4   THEN 'Y' ELSE 'N' END AS NN,
    CASE WHEN c.PEPBitwiseCode & 8   = 8   THEN 'Y' ELSE 'N' END AS ForeignPEP,
    CASE WHEN c.PEPBitwiseCode & 16  = 16  THEN 'Y' ELSE 'N' END AS LocalPEP,
    CASE WHEN c.PEPBitwiseCode & 32  = 32  THEN 'Y' ELSE 'N' END AS ForeignOrgPEP,
    CASE WHEN c.PEPBitwiseCode & 64  = 64  THEN 'Y' ELSE 'N' END AS EfPEP,
    CASE WHEN c.PEPBitwiseCode & 128 = 128 THEN 'Y' ELSE 'N' END AS CustRPIsPEP
FROM Customer_Migrate_buffer a
JOIN RP_NP_Migrate_buffer c    ON c.CIFKey= a.CIF_KEY
join CIF_Migrate_Mapping_TEMP v on v.SOURCE_CIF_KEY = a.CIF_KEY
join BSA_SUB_CUST_FACT bb on bb.CIF_KEY = v.NEW_CIF_KEY
join PB_CUSTOMER_FACT cc on cc.CIF_KEY = bb.CIF_KEY
WHERE  c.PEPBitwiseCode = '4'  AND cc.CUST_TYPE_CDE = 'N' AND v.ACTION = 'insert' AND c.RP = 'OTH'



---負面新聞 企業 法定代理人
-------------------------------------------------------- LA 法定代理人



SELECT 
    a.CustomerNo,
    c.RefNo,
    c.PEPBitwiseCode,
    CASE WHEN c.PEPBitwiseCode & 1   = 1   THEN 'Y' ELSE 'N' END AS BlackList,
    CASE WHEN c.PEPBitwiseCode & 2   = 2   THEN 'Y' ELSE 'N' END AS PEP,
    CASE WHEN c.PEPBitwiseCode & 4   = 4   THEN 'Y' ELSE 'N' END AS NN,
    CASE WHEN c.PEPBitwiseCode & 8   = 8   THEN 'Y' ELSE 'N' END AS ForeignPEP,
    CASE WHEN c.PEPBitwiseCode & 16  = 16  THEN 'Y' ELSE 'N' END AS LocalPEP,
    CASE WHEN c.PEPBitwiseCode & 32  = 32  THEN 'Y' ELSE 'N' END AS ForeignOrgPEP,
    CASE WHEN c.PEPBitwiseCode & 64  = 64  THEN 'Y' ELSE 'N' END AS EfPEP,
    CASE WHEN c.PEPBitwiseCode & 128 = 128 THEN 'Y' ELSE 'N' END AS CustRPIsPEP
FROM Customer_Migrate_buffer a
JOIN RP_NP_Migrate_buffer c    ON c.CIFKey= a.CIF_KEY
join CIF_Migrate_Mapping_TEMP v on v.SOURCE_CIF_KEY = a.CIF_KEY
join BSA_SUB_CUST_FACT bb on bb.CIF_KEY = v.NEW_CIF_KEY
join PB_CUSTOMER_FACT cc on cc.CIF_KEY = bb.CIF_KEY
WHERE  c.PEPBitwiseCode = '4'  AND cc.CUST_TYPE_CDE = 'P' AND v.ACTION = 'insert' AND c.RP = 'LA'




-- 負面新聞 企業 董事會成員
-------------------------BOD 董事會成員
SELECT 
    a.CustomerNo,
    c.RefNo,
    c.PEPBitwiseCode,
    CASE WHEN c.PEPBitwiseCode & 1   = 1   THEN 'Y' ELSE 'N' END AS BlackList,
    CASE WHEN c.PEPBitwiseCode & 2   = 2   THEN 'Y' ELSE 'N' END AS PEP,
    CASE WHEN c.PEPBitwiseCode & 4   = 4   THEN 'Y' ELSE 'N' END AS NN,
    CASE WHEN c.PEPBitwiseCode & 8   = 8   THEN 'Y' ELSE 'N' END AS ForeignPEP,
    CASE WHEN c.PEPBitwiseCode & 16  = 16  THEN 'Y' ELSE 'N' END AS LocalPEP,
    CASE WHEN c.PEPBitwiseCode & 32  = 32  THEN 'Y' ELSE 'N' END AS ForeignOrgPEP,
    CASE WHEN c.PEPBitwiseCode & 64  = 64  THEN 'Y' ELSE 'N' END AS EfPEP,
    CASE WHEN c.PEPBitwiseCode & 128 = 128 THEN 'Y' ELSE 'N' END AS CustRPIsPEP
FROM Customer_Migrate_buffer a
JOIN RP_NP_Migrate_buffer c    ON c.CIFKey= a.CIF_KEY
join CIF_Migrate_Mapping_TEMP v on v.SOURCE_CIF_KEY = a.CIF_KEY
join BSA_SUB_CUST_FACT bb on bb.CIF_KEY = v.NEW_CIF_KEY
join PB_CUSTOMER_FACT cc on cc.CIF_KEY = bb.CIF_KEY
WHERE  c.PEPBitwiseCode = '4'  AND cc.CUST_TYPE_CDE = 'N' AND v.ACTION = 'insert' AND c.RP = 'BOD'


--黑名單 企業 董事會成員
-- 負面新聞 企業 董事會成員
-------------------------BOD 董事會成員
SELECT 
    a.CustomerNo,
    c.RefNo,
    c.PEPBitwiseCode,
    CASE WHEN c.PEPBitwiseCode & 1   = 1   THEN 'Y' ELSE 'N' END AS BlackList,
    CASE WHEN c.PEPBitwiseCode & 2   = 2   THEN 'Y' ELSE 'N' END AS PEP,
    CASE WHEN c.PEPBitwiseCode & 4   = 4   THEN 'Y' ELSE 'N' END AS NN,
    CASE WHEN c.PEPBitwiseCode & 8   = 8   THEN 'Y' ELSE 'N' END AS ForeignPEP,
    CASE WHEN c.PEPBitwiseCode & 16  = 16  THEN 'Y' ELSE 'N' END AS LocalPEP,
    CASE WHEN c.PEPBitwiseCode & 32  = 32  THEN 'Y' ELSE 'N' END AS ForeignOrgPEP,
    CASE WHEN c.PEPBitwiseCode & 64  = 64  THEN 'Y' ELSE 'N' END AS EfPEP,
    CASE WHEN c.PEPBitwiseCode & 128 = 128 THEN 'Y' ELSE 'N' END AS CustRPIsPEP
FROM Customer_Migrate_buffer a
JOIN RP_NP_Migrate_buffer c    ON c.CIFKey= a.CIF_KEY
join CIF_Migrate_Mapping_TEMP v on v.SOURCE_CIF_KEY = a.CIF_KEY
join BSA_SUB_CUST_FACT bb on bb.CIF_KEY = v.NEW_CIF_KEY
join PB_CUSTOMER_FACT cc on cc.CIF_KEY = bb.CIF_KEY
WHERE  c.PEPBitwiseCode = '1'  AND cc.CUST_TYPE_CDE = 'N' AND v.ACTION = 'insert' AND c.RP = 'BOD'


如圖，他要這個表格資料，但我現在SQL都是拆開寫 然後很亂，你能幫我一次寫好?
